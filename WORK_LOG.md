# Work Log

Chronological record of merged PRs and closed issues. Maintained by the Guide triage agent.

---

### 2026-06-09

- **Issue #382**: rolled the #295/#296 project-org model out to `anvil:deck`, `anvil:slides`, and `anvil:proposal` (the parallel epic to the memo-only #295/#296/#297 rollout), and extended `anvil:project-migrate` to handle their flat threads. Canary trigger: the studio's `series-a-deck` thread followed deck SKILL.md literally and produced the pre-#295 flat shape; the hand-fix (studio commit `2cf3f37`) nested the version dirs manually.
  - **Lib promotion (factoring A — resolved at curation, third-consumer trigger).** `latest_resolution.py`, `project_discovery.py`, `project_brief.py`, `cross_thread_refs.py` moved verbatim from `anvil/skills/memo/lib/` → `anvil/lib/` (intra-lib imports updated to `anvil.lib.*`). Memo paths are now back-compat shims (`from anvil.lib.X import *`, plus explicit re-export of the non-`__all__` module attributes `BRIEF_FILENAME` / `DOCUMENTS_FRONTMATTER_KEY` historically importable from the memo project_brief path). Every pre-existing import path keeps working — qualified (`anvil.skills.memo.lib.X`, per PR #383's qualification sweep), sys.path-injected top-level (memo test convention), and the post-install qualified path pinned by PR #380's `test_install_skill_lib_callable.py`. `rubric_overlays.py` stays memo-local (overlay JSONs are memo-artifact-type-specific); `load_recommendation_target`'s proposal-side mirror stays per #356's "promote when a third skill adopts" note. `anvil/lib/render_gate.py`'s tier-2 theme discovery drops the memo-lib sys.path injection for plain sibling imports. Two doc tests that pin project_brief.py source text repointed at the canonical path.
  - **SKILL.md artifact contracts (3 files).** deck/slides/proposal §"Artifact contract" trees rewritten to the nested `<project>/<slug>/<slug>.N/` shape with the project-level `BRIEF.md` documented as the config locus per #296. **Slug-echo body rename scoped OUT** (resolved at curation): `deck.md` is consumed by marp CLI invocations + templates; `proposal.tex` by xelatex + `anvil-proposal.cls`. Each SKILL.md carries an explicit body-filename note retaining the skill-fixed name and flagging slug-echo as a follow-on. Deck/slides iteration-cap prose names the project-level BRIEF as the v2 carrier-convergence target while `.anvil.json` stays the honored v1 carrier.
  - **Proposal `.anvil.json` cleanup per #296.** The four SKILL.md references retired: tree entry removed; iteration cap repointed at the project-BRIEF paired override (schema-of-record `anvil/lib/project_brief.py`, with legacy `.anvil.json` reads documented as honored until the proposal-side BRIEF reader lands); render-gate `page_cap` flagged TBD-on-BRIEF-grow mirroring memo's `words_per_page` prose; Defaults bullet folded into the BRIEF.md.example entry. `customer_kind` stays on the thread-level BRIEF frontmatter for v1 (resolved at curation).
  - **`project-migrate` extension.** `detect.py`: classic-thread walk records a per-thread `.anvil.json` inside a sibling thread root (the nested-but-flat deck shape); FULLY_MIGRATED body check relaxed from "every body is `<slug>.md`" to "no skill-fixed body present" (load-bearing for idempotence on migrated deck projects — `deck.md` + `speaker-notes.md` would otherwise block the no-op classification forever); `deck.md` / `proposal.tex` deliberately NOT added to `_SKILL_FIXED_BODY_FILENAMES` (body rename is a no-op for those skills). `plan.py`: `_plan_pre_283_doc` covers nested-but-flat (thread-root contents stay, version dirs + critic siblings move in); thread-root `.anvil.json` claimed ahead of the project-root one; `BriefMergeOp` gains the paired `max_iterations` + `iteration_cap_rationale` fields, carried only when the pair would survive the strict BRIEF parser (int ≥ 4 + non-empty rationale; unpaired/default-cap shapes dropped with a plan note); POST_283 mixed-grammar projects dispatch per thread (flat → nesting plan, nested → in-place cleanup); retained-body threads get an operator note (no body rename + `artifact_type` defaulted to `investment-memo` — the registered enum is memo-scoped in v1). `apply.py`: `_write_project_brief` serializes the iteration-cap pair (literal block scalar) and preserves it on pre-existing operator entries; the rename/git-mv machinery is untouched. `verify.py`: no changes needed — layout-generic, now round-trips through the promoted `anvil.lib.project_discovery`.
  - **Fixtures + tests.** Programmatic builders (per the skill's `_fixtures.py` convention): `build_aldus_shaped_deck` (sanitized pre-`2cf3f37` snapshot; `with_project_brief` flag for the mixed-grammar POST_283 case) and `build_mixed_memo_deck_proposal` (flat memo + deck + proposal in one root). Four new test files: `test_project_migrate_detect_mixed.py`, `test_project_migrate_plan_mixed.py`, `test_project_migrate_apply_mixed.py` (includes the cross-skill discovery smoke through `anvil.lib.project_discovery` for all three thread types), `test_project_migrate_idempotent_mixed.py` (re-apply zero-diff via tree digest). `test_project_migrate_verify.py` gains the mixed round-trip including strict-parser survival of the merged iteration-cap pair. New `tests/lib/test_{latest_resolution,project_discovery,project_brief,cross_thread_refs}.py` pin the canonical import path + shim identity (the memo corpora keep exercising the canonical implementation through the shims, which re-export the same function objects).
  - **Touched**: MOVED 4 modules `anvil/skills/memo/lib/{latest_resolution,project_discovery,project_brief,cross_thread_refs}.py` → `anvil/lib/` (+ 4 shim files), MODIFIED `anvil/lib/render_gate.py`, MODIFIED `anvil/skills/{deck,slides,proposal}/SKILL.md`, MODIFIED `anvil/skills/project-migrate/{SKILL.md,lib/detect.py,lib/plan.py,lib/apply.py}`, MODIFIED `anvil/skills/project-migrate/tests/{_fixtures.py,test_project_migrate_verify.py}`, NEW 4 mixed-shape test files + 4 `tests/lib/` canonical-path test files, MODIFIED 2 doc tests (`tests/skills/memo/`), MODIFIED `CHANGELOG.md` + this `WORK_LOG.md`.
  - **Out of scope (per curation, tracked as follow-ons)**: slug-echo body rename for deck/slides/proposal; installation/ip-uspto/pub/report rollout; rubric-overlay promotion; `load_recommendation_target` promotion; project-BRIEF schema growth for deck knobs (`stage`, `imagery_policy`, `imagery_style`, `customer_kind`); per-skill artifact-type enum growth (migrated non-memo threads default to `artifact_type: investment-memo` with a plan note).

### 2026-06-05

- **Issue #342 (Epic #328 Track B Phase 6)**: shipped the `claim-figure-grounding` critic for the `anvil:report` skill as a deterministic `tool_evidence`-class pre-flight detector. The critic scans the body markdown of a report version dir for prose that promises a figure / table / chart and validates the referenced label against an on-disk roster discovered from three ground-truth sources (LaTeX `\label{}` macros, markdown pandoc-style `{#prefix:id}` anchors, and `figures/` + `exhibits/` filenames). Missing references emit a `major`-severity `Finding` with a closest-match suggestion (numeric distance for integer ids; `difflib.get_close_matches` for alphabetic ids — class-restricted) plus a top-level `critical_promised_figure_missing` `CriticalFlag` that short-circuits the aggregator's verdict to `BLOCK`. Phase 6 closes out Epic #328 Track B alongside the in-flight Phase 4 / Phase 5 sibling deferred phases (#341 image-accessibility, #340 figure-content) — coordinated with #340 on shared figure-discovery primitives (the lift to a `figure_discovery` helper is deferred per the issue body's "do not promote in this PR" guidance; ship inline until a second consumer surfaces).
  - **`anvil/skills/report/lib/claim_figure_grounding.py`** new module — owns the full pipeline: pure-function `scan(body, known_labels)` over a body string and a set of `(label_class, label_id)` tuples; `collect_known_labels(version_dir)` walks the three ground-truth sources (recursive on `.md` / `.tex` / `.latex` for LaTeX macros + markdown anchors; non-recursive on `figures/` and `exhibits/` for filename-derived labels) with graceful-degrade on unreadable files / unrecognized prefixes; `scan_version_dir(version_dir, body_filename=None)` is the convenience wrapper that bridges the disk → pure-function path (defaults body filename to `report.md` per the report skill's Artifact contract); `_cli_main` is the `python -m` entry point that writes `_review.json` + `_findings.json` into the `.claim-figure-grounding/` sibling. Three prose-detection regex classes (prepositional / subject-verb / parenthetical) over three label-id forms (integer / dotted / single uppercase letter); the canonical class vocabulary (`Figure` / `Table` / `Chart`) normalizes prose-form abbreviations (`Fig.` → `Figure`; `Tbl.` → `Table`). False-positive disciplines mirror citation-coverage (quoted material suppression: blockquotes, fenced code blocks, inline backticks; same-line same-`(class, id)` collapse at detection time). The dedupe contract groups references by `(label_class, label_id)` and emits one finding per missing label with the first reference's line + verbatim text anchoring the evidence span and an `additional_references: N` count in the rationale. Skill-local first per CLAUDE.md §"Skill-local first, lib promotion later" — promotion to `anvil/lib/` is deferred until #340 lands and a `figure_discovery` promotion candidate is filed.
  - **`anvil/skills/report/commands/report-claim-figure-grounding.md`** new command doc — documents the CLI entry-point (`python -m anvil.skills.report.lib.claim_figure_grounding <version_dir> [--write-review] [--body-filename <name>]`), the three detection-class table with examples, the recognized class words / verbs / id forms, the three ground-truth roster sources, the closest-match suggestion algorithm (numeric distance ≤ 2 for integer ids; `difflib.get_close_matches` cutoff 0.6 for alphabetic ids; class-restricted), the dedupe contract, the false-positive disciplines, the critical-flag heuristic ("any missing → flag"), the auto-discovery wiring (`<version_dir>.claim-figure-grounding/` sibling matches the `<version_dir>.<tag>/` pattern), the exit codes (`0` clean / `1` findings / `2` invocation error), the failure-mode table, and the re-run patterns. Composability with the standard report lifecycle is documented inline — the critic runs after `report-draft` and before `report-review`; the aggregator picks up the sibling automatically.
  - **`tests/skills/report/test_claim_figure_grounding.py`** new test file — 59 tests covering the full acceptance criteria. Module-surface guards (constants, `total_findings`, `to_review` shape with `kind=tool_evidence` + single null-scored row + `tool_calls=[]` on every finding). Positive cases for all three prose-detection shapes (prepositional `see Figure N` / `as shown in Chart B` / `per Table 2` / `in Figure 3.1`; subject-verb `Figure 3 illustrates` / `Table 2 reports` / `Chart 1 shows`; parenthetical `(Figure 3)` / `(Table 2)`); positive cases for the abbreviation `Fig. 3` form normalizing to `Figure 3`. Grounded paths for all three ground-truth sources: LaTeX `\label{fig:3}` in body markdown; markdown `{#fig:3}` anchor on a heading; filename `figure-3.png` in `figures/`; filename `fig-3.svg` in `exhibits/`; dotted-id filename `figure-3-1.png` for `Figure 3.1`; alphabetic filename `chart-a.png` for `Chart A`; non-PNG filename `table-2.md` for `Table 2`. Closest-match suggestion (numeric distance 1 → suggest; distance 2 → still suggest; distance ≥ 3 → no suggestion; class-restricted: `Figure 3` referenced with only `Table 3` in roster → no cross-class suggestion; dotted-id `Figure 3.2` referenced with `Figure 3.1` in roster → suggests via difflib). Dedupe contract (two references one finding; distinct labels separate findings; first-reference evidence-span anchoring). False-positive disciplines (blockquoted / fenced-code / inline-backtick / same-line dupes). Label-roster discovery (LaTeX macros; markdown anchors; `figures/` filenames; `exhibits/` filenames; dotted filename normalization; ignoring `\label{sec:intro}` and other non-figure prefixes; ignoring non-label files like `README.md` in `figures/`). Critical-flag heuristic (fires on any missing; does not fire on clean; justification lists first three missing with `+N more`). Filesystem integration (`scan_version_dir` end-to-end; missing body returns empty; body-filename override works). Auto-discovery contract (sibling discovered by `discover_critics`; review round-trips through `load_review`; aggregate merges findings + critical flag and the verdict short-circuits to `BLOCK`). CLI entry-point shape (write-review opt-in; clean-scan-exits-zero; missing-dir-exits-two; grounded body exits zero). Doc-coverage tests pinning the command doc's references to the lib module path, the critical-flag type, the sibling-dir naming convention, and the three ground-truth source mentions.
  - **CHANGELOG.md `[Unreleased]` § Added** entry — documents the shipped contract, the three detection classes, the three ground-truth roster sources, the closest-match suggestion algorithm, the dedupe contract, the critical-flag heuristic, the false-positive disciplines, the CLI entry-point shape, the skill-local-first posture, and the zero-touch contract on `anvil/lib/`. The entry is positioned at the top of `[Unreleased]` § Added.
  - **Scope discipline preserved (the load-bearing constraint).** Zero `anvil/lib/` changes. Zero schema changes in `anvil/lib/review_schema.py`. Zero changes to other skills (`memo`, `pub`, `deck`, `slides`, `ip-uspto`, `installation`, `proposal`, `project-migrate`). Zero changes to the `report` skill's `rubric.md` or existing commands (`report-audit.md` / `report-draft.md` / `report-figures.md` / `report-promote.md` / `report-review.md` / `report-revise.md` / `report-vision.md`). Report-first per the issue body's explicit out-of-scope list — no memo-side claim-figure-grounding port. The new module and command live entirely inside `anvil/skills/report/`; the new test file lives entirely inside `tests/skills/report/` (with a new `__init__.py` to seal the `tests/skills/report/` namespace per the cross-skill packaging convention #58).
  - **Coordination with sibling deferred phases.** Per the issue body's coordination notes: (a) #340 (figure-content) touches similar figure-discovery logic (walking the version dir for LaTeX `\label{}` + markdown `{#fig:}` extension + `figures/` subdir filenames); if #340's builder writes the same discovery code, a follow-on `figure_discovery` promotion issue can be filed — **not promoted in this PR** per the issue's "ship inline" directive. (b) #341 (image-accessibility) has independent file scope; no overlap. (c) All three deferred-phase critics share the CLI shape `python -m anvil.skills.report.lib.<module> <version_dir> [--write-review]` per the Phase 2 (#338) precedent — picked up faithfully here.
  - **Critical-flag posture.** Any missing reference fires `critical_promised_figure_missing`. This is stricter than the citation-coverage threshold (`>5` OR named-author short-circuit) — and load-bearing for customer-facing reports: a single broken `see Figure 3` reference in a delivered report is a credibility defect the operator must address. The aggregator's `compute_verdict` short-circuits to `Verdict.BLOCK` on the flag (validated by the auto-discovery roundtrip test).
  - **Touched**: NEW `anvil/skills/report/lib/claim_figure_grounding.py`, NEW `anvil/skills/report/commands/report-claim-figure-grounding.md`, NEW `tests/skills/report/test_claim_figure_grounding.py`, NEW `tests/skills/report/__init__.py`, MODIFIED `CHANGELOG.md` `[Unreleased]` entry, MODIFIED this `WORK_LOG.md` entry. No changes to `anvil/lib/`, no schema changes, no other-skill changes, no changes to existing report-skill files.
  - **Test results**: full suite green — 1332 passed, 2 skipped (network smoke tests under `tests/lib/`). The 59 new tests in `tests/skills/report/test_claim_figure_grounding.py` cover the module surface, all three prose-detection shapes (positive + grounded paths), all three label-id forms, all three ground-truth roster sources, the closest-match suggestion algorithm (numeric + alphabetic + dotted + class-restricted), the dedupe contract, the false-positive disciplines, the critical-flag heuristic, the filesystem integration via `scan_version_dir`, the auto-discovery contract through `discover_critics` + `load_review` + `aggregate`, the CLI entry-point exit-code semantics, and the doc-coverage suite.

- **Issue #340 (Epic #328 Phase 4 — figure-content VLM critic)**: shipped the figure-content VLM critic at `anvil/lib/figure_content.py` as the first of three deferred-phase critics in the Epic #328 wave (sibling phases #341 image-accessibility and #342 claim-figure-grounding ship in parallel). The critic runs a vision-language-model pass over every figure in a memo or report version directory and scores three axes per figure: on-brand palette match (vs `anvil/lib/figures/palette.py`), caption-claim grounding, adjacency-claim grounding. Direct-lib placement (NOT skill-local) — both `memo` and `report` consume from day one, satisfying CLAUDE.md's "wait for the second consumer" rule at ship. Critical-flag `critical_figure_misrepresents_claim` fires on VLM-detected caption-vs-figure contradiction and forces `Verdict.BLOCK` via the aggregator's `compute_verdict` short-circuit.
  - **`anvil/lib/figure_content.py`** new module (~900 lines including docstrings). Composes the existing `VisionCritic` / `VisionRubric` substrate from `anvil/lib/vision.py` — no VLM-call infrastructure reimplementation. Two figure-discovery paths: (a) PDF page extraction via `pdftoppm` subprocess (delegates to `anvil.lib.render.render_pdf_to_pngs`; graceful-degrades when the tool is missing — top-level `reason` + zero findings, mirroring the `check_*_available()` family in `anvil/lib/render.py`); (b) `<version_dir>/figures/` walk for direct PNG / JPG / WebP sources (SVG sources recorded as unverified via top-level reason — the VLM consumes raster bytes, no svg2png conversion shipped). Per-figure VLM cost cap (default 1 call per figure per run, configurable via `vlm_budget_per_figure=N` on `critique_version_dir`). Session-lifetime in-process content-hash cache (`sha256(figure_bytes) → VLM payload`) so a 30-page customer report with a repeated header logo costs 1 VLM call total, not 30 — load-bearing for the figure-heavy `report` consumer specifically. Cache shipped inline in `figure_content.py` (`FigureVLMCache` class); promotion to `anvil/lib/vision_cache.py` is deferred until Phase 5 (#341) reaches for the same shape per the issue body's coordination note. Per-figure scores roll up to three rubric rows via per-dimension mean (rounded to nearest int via round-half-to-even) so the `_review.json` reads as a single 3-row scorecard, total /15. Sub-threshold dims (score ≤ max/2) auto-emit a `Finding` even when the VLM omits a narrative entry (safety net for "low score, no narrative entry"). Auto-discovery via the existing `<version_dir>.<tag>/` contract — `.figure-content/` sibling is picked up by `anvil/lib/critics.py::discover_critics` with no aggregator changes (third consumer of the convention, after Phase 2's `.hyperlinks/` and Phase 3's `.citations/`).
  - **`anvil/skills/memo/commands/memo-figure-content.md`** new command doc (~220 lines). Documents the CLI shape (`python -m anvil.lib.figure_content <version_dir> [--write-review]`), the three scoring axes with off-axis signal table, the severity ladder (4-5 clean / 3 minor / 2 minor / 1 major / 0 major / contradiction blocker via critical flag), the seven failure modes with operator actions (caption misrepresents / off-brand palette / non-sequitur figure / pdftoppm unavailable / no figures / SVG-only / missing version_dir), the auto-discovery wiring, the cross-skill consumer parity rationale, and the load-bearing cost-discipline notes.
  - **`anvil/skills/report/commands/report-figure-content.md`** new command doc (~200 lines). Mirrors the memo doc shape (CLI / axes / severity / failure modes / auto-discovery / cross-skill parity) with report-specific framing: reports are figure-heavy, so the content-hash cache + per-figure budget cap are load-bearing for cost control on this consumer; the audit phase that produces `CUSTOMER-READY` should fail when a figure-content critical flag is present.
  - **`tests/lib/test_figure_content.py`** new test file (43 tests, ~700 lines). Coverage: three-axis rubric shape (each /5, max_total=15; rubric_id pinned; sibling suffix matches critic_id with no dots); prompt construction (rubric dim names + critical-flag taxonomy + caption + adjacency + ANVIL_NAVY hex appears in palette section); figure discovery (empty version dir → empty records + "(none)" rendered_artifact placeholder so Review schema validates; figures/ dir PNGs → distinct hashes for distinct bytes; SVG-skip with reason; pdftoppm graceful-degrade preserves figures/ dir discovery); the three scoring axes (on-brand clean = no finding, off-brand low score = finding emitted; caption clean = no finding, misleading caption = finding; adjacency clean = no finding, non-sequitur = finding + drop/replace suggestion in suggested_fix); VLM cache deduplication (identical content → 1 VLM call for 2 figures; distinct content → 2 calls; external cache lives across `critique_version_dir` calls); critical-flag firing on contradiction + Verdict.BLOCK short-circuit on aggregate; Review schema compliance (kind=vision + rendered_artifact required by schema validator at review_schema.py:371 + round-trip through model_validate + empty-critique → null-scored rubric rows); auto-discovery + aggregate round-trip; per-figure score roll-up to rubric mean (e.g. [5,5,5] + [1,3,5] → [3,4,5] total 12); CLI smoke (exit 0 clean / 1 findings / 2 invocation error + --write-review creates sibling dir); top-level reasons (empty version dir + to_json shape includes all expected fields); pdftoppm preflight (returns bool); command-doc lock-in (both memo and report docs ship with CLI shape, sibling-dir name, critical-flag type name); VLM budget cap (budget=0 skips VLM with top-level reason). **All VLM calls are MOCKED** via the `callback=` injection point on `VisionCritic` — no network access required for the test suite, matching the Phase 2 / Phase 3 precedent.
  - **Schema-delta discipline preserved.** Phase 4 settle holds: zero changes to `anvil/lib/review_schema.py`. The `Finding` model lock-in test (`test_schema_delta_did_not_creep`) asserts the deferred experimental fields (`action`, `target_anchor`, `proposed_content`) have NOT crept in — matches the Phase 2 (#338) and Phase 3 (#337) regression guards. Every emitted `Finding` uses the existing free-form `suggested_fix` text.
  - **Out-of-scope discipline preserved.** Zero changes to `anvil/skills/memo/rubric.md` or `anvil/skills/report/rubric.md` (the critic emits findings; the rubric reads them at review time via the aggregator's auto-discovery, no rubric edits needed). Zero changes to existing skill command step structures (the `memo-review` step structure renumbered by PR #332 is left alone). Zero changes to other skills (slides / deck / ip-uspto / etc. — memo + report only per the AC).
  - **Direct-lib placement rationale (settled at issue body; do NOT re-litigate).** The CLAUDE.md "skill-local first, lib promotion later" rule explicitly carves out the two-consumer threshold: the rule is "wait for the *second* consumer before generalizing", which is satisfied at ship because both `memo` and `report` reach for the same primitive on day one. Phase 2 (`hyperlink_resolver`, #335) and Phase 3 (`citation_coverage`, #336) shipped skill-local because their second consumer hadn't surfaced; Phase 4 ships direct-lib because it has.
  - **Coordination with sibling deferred phases (#341, #342).** All three share the CLI shape `python -m <module> <version_dir> [--write-review]` (exit codes 0 clean / 1 findings / 2 invocation error) and the `<version_dir>.<tag>/` sibling-dir convention. The content-hash cache is shipped **inline** in `figure_content.py` for now per the issue body's coordination note; if #341's builder reaches for the same shape, they can coordinate via comments and file a follow-on `vision_cache.py` promotion. Similarly the figure-discovery walker is shipped inline; #342 (`claim-figure-grounding`) uses similar discovery logic and will mirror the implementation until duplication is observed.
  - **VLM cost discipline.** Default budget: 1 VLM call per figure per run. Content-hash cache makes repeat figures (logos / charts that appear on multiple pages) free after the first call. For a figure-heavy customer report this is the difference between "30 VLM calls for 30 pages with a header logo" and "1 VLM call for the unique logo + per-figure calls for the others". The budget is configurable so a future canary tuning (e.g. noise-reduction sampling at vlm_budget_per_figure=3) doesn't require a code change.
  - **Touched**: NEW `anvil/lib/figure_content.py` (~900 lines), NEW `anvil/skills/memo/commands/memo-figure-content.md` (~220 lines), NEW `anvil/skills/report/commands/report-figure-content.md` (~200 lines), NEW `tests/lib/test_figure_content.py` (43 tests, ~700 lines), MODIFIED `CHANGELOG.md` [Unreleased] § Added entry, MODIFIED this `WORK_LOG.md` entry. No changes to `anvil/lib/review_schema.py`, no changes to `anvil/lib/vision.py` (composed only), no changes to existing skill rubrics, no changes to existing skill command step structures, no changes to other skills.
  - **Test results**: full suite green — 1316 passed, 2 skipped (network smoke tests under `tests/lib/`). The 43 new tests in `tests/lib/test_figure_content.py` cover every #340 acceptance criterion (rubric + prompt + discovery + three axes + cache + critical flag + review shape + auto-discovery + score aggregation + CLI + reasons + pdftoppm preflight + command-doc lock-in + VLM budget) with all VLM calls mocked. Total test count up from 1273 (pre-PR) to 1316.

- **Issue #341 (Epic #328 Phase 5)**: shipped the `image-accessibility` critic for the `anvil:memo` skill as a tool-evidence + VLM-hybrid pre-flight detector. The critic scans the body markdown of a memo version dir for three accessibility defect classes — missing alt text, inadequate (placeholder / sub-10-char) alt text, and broken image paths — and emits a typed `Review` (`kind=Kind.TOOL_EVIDENCE`) into the auto-discovered `<version_dir>.image-accessibility/` sibling. Phase 5 of the reactivated Epic #328 (the deferred phases reactivated 2026-06-05 to ship the full epic before v0.3.2 release), shipping alongside the sibling Phase 4 (`figure-content`, #340) and Phase 6 (`claim-figure-grounding`, #342) — all three using the same CLI shape (`python -m anvil.skills.memo.lib.<module> <version_dir> [--write-review]`) per the Phase 2 (#338) precedent.
  - **`anvil/skills/memo/lib/image_accessibility.py`** new module. Owns the full pipeline: pure-function `scan(source, version_dir, *, vlm_callback)` over a body string + version dir + optional VLM callback; `scan_version_dir(version_dir)` is the convenience wrapper bridging disk → pure-function; `_cli_main` is the `python -m` entry point. **Reuses existing primitives** per the issue body AC: `memo_image_refs.lint_source` for path-existence detection (no duplicate filesystem walking — the only filesystem touch for existence is the one already paid by the lint), `anvil/lib/vision.py` for the VLM primitive (callback-injectable per the existing vision-critic contract), `anvil/lib/review_schema.py` for canonical `Review`/`Finding`/`ToolCall` shapes (no schema delta — settled at Epic #328 kickoff). Three finding classes with the severity ladder from the issue body: missing alt → `major`; inadequate alt → `minor`; broken path → `major`. **Class precedence**: broken-path takes priority over alt-quality (one ref, one finding — if the file is missing, the alt discussion is moot). **No critical flags** — a11y is advisory in v0. **Inadequate-alt heuristics** catch (i) literal placeholders `image`/`figure`/`chart`/`img`/`picture`/`graphic`/`diagram` (the issue body's named list, expanded for sibling shapes), (ii) generic prefixes alone like `screenshot`/`photo`/`illustration`/`drawing`/`icon` (passes when a further subject follows, so `screenshot of OAuth flow` is fine but `screenshot` alone is not), (iii) sub-`INADEQUATE_ALT_MIN_LENGTH`=10-character alts with trailing punctuation stripped before measurement. **Broken-path closest-match** uses `difflib.get_close_matches` with cutoff `0.6` (mirrors `citation_coverage`'s value so the two stdlib-difflib consumers feel uniform) — tries the full ref first, falls back to basename so the `cp -r exhibits/ <version_dir>/` footgun surfaces correctly. **Suppression directives** `<!-- anvil-lint-disable: memo_image_accessibility_{missing_alt,inadequate_alt,broken_path} -->` honored same-line + line-above, mirroring the `memo_image_refs_exist` discipline. **VLM cost discipline**: content-hash cache (process-local dict, session-lifetime) ensures `sha256(image_bytes)` collisions reuse the first VLM result. Per the issue body's coordination note with Phase 4 (#340 inline cache shape), the dict-shape is intentionally simple so a future `anvil/lib/vision_cache.py` promotion is a one-line import swap. **CLI is OFFLINE-safe by default**: the CLI does NOT invoke the VLM; missing/inadequate-alt findings still fire but with deterministic template `suggested_fix` text. Programmatic consumers pass a callback to `scan` / `scan_version_dir` for VLM-generated candidates. Skill-local first per CLAUDE.md — promotion to `anvil/lib/` is deferred until a second skill (`pub`, `report`, `proposal`) reaches for the same primitive.
  - **Kind-per-finding decision (open question from issue body): single sibling, single `Kind.TOOL_EVIDENCE`.** The schema validator enforces (a) `kind=VISION` requires `rendered_artifact` set on the Review (one rendered artifact per Review), (b) `kind=TOOL_EVIDENCE` requires `tool_calls` on every Finding. The image-accessibility critic spans N images per memo (one per ref); the N-images-per-Review shape fits TOOL_EVIDENCE (per-finding `tool_calls` records the per-image VLM invocation) and is a structural mismatch for VISION (which would require one `rendered_artifact` per Review). Decision: ship as ONE `<version_dir>.image-accessibility/` sibling with `kind=Kind.TOOL_EVIDENCE`. Broken-path findings carry `tool_calls=[]` (path-existence is reused from `memo_image_refs` — no tool invocation per finding); missing/inadequate-alt findings carry a one-entry `ToolCall` list describing the VLM invocation (model name, image path, whether the callback was invoked or short-circuited via cache/no-callback; `result_summary` carries the generated candidate or a sentinel string). This choice is load-bearing for the test suite — the round-trip through `Review.model_validate` succeeds only because every Finding emitted carries `tool_calls` when `kind=tool_evidence`. Decision documented in both the module docstring (§"Architecture & design decisions") and the command doc (§"Kind-per-finding decision").
  - **`anvil/skills/memo/commands/memo-image-accessibility.md`** new command doc. Documents the role + reads + writes contracts; the three classes with severity table; the kind-per-finding decision rationale; the VLM coordination + cost discipline; the auto-discovery contract (the new tag `image-accessibility` contains a hyphen but no dot, so the discovery regex matches without changes); the severity ladder (no critical flags); the suppression directive (three rule names, same-line + line-above); the CLI entry-point with exit codes; the failure modes table; the re-run patterns; and the composability with the standard memo lifecycle. Mirrors the structural shape of `memo-citations.md` (#337) and `memo-hyperlinks.md` (#338) for consistency across the Track B / deferred-phase critic family.
  - **`tests/skills/memo/test_image_accessibility.py`** new test file — 46 tests covering all eight acceptance criteria. VLM calls are MOCKED via `unittest.mock.Mock(return_value=...)` per the AC. Module-surface guards (constants match docs). AC1 — valid alt produces no finding (markdown + HTML + no-refs-at-all). AC2 — missing alt fires (markdown empty alt, HTML no-attribute, HTML empty alt-string) with VLM candidate landing in `suggested_fix`; no-callback path falls back to the deterministic template. AC3 — placeholder alt fires (`image`, `figure`, `chart`, `screenshot` alone vs with subject, case-insensitive `IMAGE`). AC4 — sub-10-char alt fires; 10-char boundary passes; trailing punctuation stripped before length check. AC5 — broken path with no nearby match emits propose_removal; broken-path-takes-priority-over-inadequate-alt regression guard. AC6 — broken path with nearby match emits propose_edit with closest-match (cp -r footgun shape `exhibits/foo.png` → `foo.png` at root; typo `revenue-fy42.png` → `revenue-fy24.png`). AC7 — VLM cache deduplicates identical bytes (one callback invocation for two PNGs with identical content; two callbacks for distinct bytes); `clear_vlm_cache` resets state; `generate_alt_text` direct invocation caches. AC8 — auto-discovery via `discover_critics` + `load_review` + `aggregate` round-trip producing a clean `AggregatedReview` with no critical flags; schema-discipline regression guard asserting every Finding carries `tool_calls`. Plus: suppression directive (same-line + line-above), URL/absolute-path skipping, result serialization (`to_json` shape, `to_review` no-critical-flags discipline), CLI smoke (clean → 0, findings → 1, missing dir → 2, `--write-review` opt-in, default no-write), graceful-degrade on missing body file and raising-VLM-callback.
  - **Touched**: NEW `anvil/skills/memo/lib/image_accessibility.py` (~720 lines), NEW `anvil/skills/memo/commands/memo-image-accessibility.md` (~140 lines), NEW `tests/skills/memo/test_image_accessibility.py` (~700 lines / 46 tests), MODIFIED `CHANGELOG.md` [Unreleased] entry, MODIFIED this `WORK_LOG.md` entry. Zero `anvil/lib/` changes. Zero `anvil/lib/review_schema.py` changes (no schema delta — settled at Epic #328 kickoff). Zero changes to `anvil/skills/memo/rubric.md` (Phase 1 from #333 just touched dim 3 weighting). Zero changes to `anvil/skills/memo/commands/memo-review.md` step structure (4f/4g/4h/4i load-bearing from PR #332/#334). Zero changes to `memo_image_refs.py` (reused as-is per its docstring's "reference implementation pattern" — promotion to `anvil/lib/` deferred per the issue body's "do NOT promote in this PR" instruction). Memo first.
  - **Test results**: full suite green — **1319 passed, 2 skipped** (network smoke tests under `tests/lib/`). The 46 new tests in `tests/skills/memo/test_image_accessibility.py` cover all 8 acceptance criteria from the issue body plus VLM-cache deduplication, schema-validator round-trip, suppression directives, skipped-paths, result serialization, CLI exit codes, and graceful-degrade paths.
  - **Coordination with Phase 4 (#340, `figure-content`).** Both phases touch `anvil/lib/vision.py`. The issue body called out: "if #340's builder ships an inline content-hash → VLM-result cache, you should mirror its shape (in-process dict, session-lifetime eviction). Do not promote to a shared `vision_cache.py` in this PR." Phase 5 ships an inline `_VLM_CACHE: dict[str, str]` in `image_accessibility.py` per that contract. The cache key is `sha256(image_bytes)`; eviction is session-lifetime via process exit; the `clear_vlm_cache()` test helper resets state. A future `anvil/lib/vision_cache.py` promotion is a one-line import swap when the second consumer materializes (likely the figure-content phase merging the two consumers into a shared cache primitive post-Phase-5-and-Phase-4-landing).
  - **Coordination with Phase 6 (#342, `claim-figure-grounding`).** Independent file scope — no overlap per the issue body's coordination section.

- **Issue #228 (memo: convictions ledger Resolution-type extension)**: shipped `addressed (judgment-held) — <judgment>` as a third Resolution type in the changelog-embedded convictions ledger, alongside the existing `declined`. Closes a structural gap from #226 surfaced after the `_convictions.md` primitive was killed in #229 — the prior write trigger only captured `declined` rows, missing load-bearing convictions from canary threads where the reviser *addressed* a finding but the addressing judgment was structural (the $450B-as-anchor-not-TAM reframe, the 6-risks-is-enough scope limit, the bear/base/bull sufficiency call). The new type inherits the #225 fix automatically since `changelog.md` is always written regardless of `verdict.advance`. Single-file documentation edit (~14 lines) to `anvil/skills/memo/commands/memo-revise.md`: CLI invariants bullet, step 7 always-include, step 9 changelog example + three-type vocabulary definition, Notes / Convictions ledger explanation. Falsifiability contract: 3-week observation window with two kill-switches (A: disuse — grep returns empty; B: ineffective — marker used but judgment re-raised without reviser acknowledgment). Originally authored on a divergent local branch in an earlier session and missed the merge to origin/main; cherry-picked onto a fresh branch from `origin/main` and PR'd properly during v0.3.2 release pre-flight.
  - **Touched**: MODIFIED `anvil/skills/memo/commands/memo-revise.md` (14 insertions / 9 deletions), MODIFIED `CHANGELOG.md` `[Unreleased]` § Added entry, MODIFIED this `WORK_LOG.md` entry. Zero `anvil/lib/` changes, zero schema changes, zero new tests (documentation-only edit; the carry-forward contract is enforced by the reviser command at runtime, not a schema-level constraint).
  - **Test results**: `tests/skills/memo/` green at 475 passed (no regressions from the doc edit).

- **Issue #336 (Epic #328 Track B Phase 3)**: shipped the `citation-coverage` critic for the `anvil:memo` skill as a deterministic `tool_evidence`-class pre-flight detector. The critic scans the body markdown of a memo version dir for unhooked load-bearing claims (numeric / named-author / quantitative summary / date-pinned events) and broken `\cite{}` / `[@]` citation keys, emitting a typed `Review` (`kind=Kind.TOOL_EVIDENCE`) into the auto-discovered `<version_dir>.citations/` sibling. Phase 3 closes out Epic #328 Track B alongside the in-flight Phase 2 (`hyperlink_resolver`, #335).
  - **`anvil/skills/memo/lib/citation_coverage.py`** new module — owns the full pipeline: pure-function `scan(body, refs_keys)` over a body string and a set of refs-side keys; `collect_refs_keys(version_dir)` walks the refs directories resolved by `refs_resolver.resolve_refs_dirs` (per-thread `<thread>/refs/` + portfolio `<portfolio>/research/`) plus the version dir's own `refs.bib` and harvests bibtex entry keys via the same regex used by `cite.py::_existing_keys`; `scan_version_dir(version_dir)` is the convenience wrapper that bridges the disk → pure-function path; `_cli_main` is the `python -m` entry point that writes `_review.json` + `_findings.json` into the `.citations/` sibling. Conservative claim-detector with four positive classes (numeric / named-author / summary / date-pinned) and four false-positive disciplines (version-context suppression, self-reference suppression, hedge suppression, quoted-material suppression — blockquotes, fenced code, inline backticks). The critical-flag heuristic emits `critical_unsourced_load_bearing_claim` when EITHER more than 5 unhooked claims surface OR any single named-author claim is unhooked (the named-author short-circuit reflects that class as intrinsically load-bearing — an unhooked one is a fabrication risk). Closest-match suggestion for broken keys via `difflib.get_close_matches` (stdlib alternative to a Levenshtein helper; `cite.py` does not ship one). Skill-local first per CLAUDE.md §"Skill-local first, lib promotion later" — promotion to `anvil/lib/` is deferred until `pub` / `report` / `proposal` reach for the same primitive (current pattern: wait for the second consumer before generalizing).
  - **`anvil/skills/memo/commands/memo-citations.md`** new command doc — documents the CLI entry-point (`python -m anvil.skills.memo.lib.citation_coverage <version_dir>`), the four detection classes with examples and default severities, the four false-positive disciplines with examples, the severity ladder mapping (broken `\cite{}` → blocker; unhooked named-author → major + critical-flag candidate; unhooked numeric / date-pinned → major; unhooked summary → minor), the critical-flag heuristic, the auto-discovery contract (the `<version_dir>.citations/` sibling is recognized by `anvil/lib/critics.py::discover_critics` without aggregator changes), and the failure modes / re-run patterns. Coordination with Phase 2 (`hyperlink_resolver`, #335) on CLI entry-point shape is documented inline: until Phase 2 lands, `python -m anvil.skills.memo.lib.<module>` is the default; if Phase 2 lands first with a different runner shape, `_cli_main` in `citation_coverage.py` is the one place to mirror it.
  - **`tests/skills/memo/test_citation_coverage.py`** new test file — 46 tests covering the full acceptance criteria. Module-surface guards (constants, `total_findings`, `to_review` shape). Positive cases for all four claim classes (numeric money, numeric percent, named-author paren shape, named-author possessive shape, date-pinned event, quantitative summary) and both citation styles (broken `\cite{ghost}`, broken `[@ghost]`). False-positive cases for all four disciplines (version 3 / Python 3.12 / Node.js 22.0.0 / see Figure 3 / Section 4 reports / roughly 30 customers / estimated $1B market / blockquoted 42 % / fenced code / inline backticks). Refs-resolution cases (valid `\cite{real-key}` clean; valid `[@real-key]` clean; line with cite marker suppresses numeric claim; multi-key `\cite{a,b,c}` resolves each; multi-key `[@a; @b]` resolves each). Closest-match suggestion (`\cite{karpathy204}` with refs containing `karpathy2024` → suggestion text contains `karpathy2024`; pandoc-style typo; unrelated key → no suggestion). Critical-flag heuristic (named-author short-circuit fires on a single instance; below-threshold count does NOT fire; above-threshold count fires; clean body emits no findings + no critical flag). Filesystem integration (`collect_refs_keys` from thread refs, version dir local bib, portfolio research; `scan_version_dir` runs the full pipeline; missing body returns empty result). Auto-discovery contract (`<version_dir>.citations/` is picked up by `discover_critics`; the typed review round-trips through `load_review`; `aggregate` merges findings without raising). CLI entry-point smoke test. Doc-coverage tests pinning the command doc's references to the lib module path, the critical-flag type, and the sibling-dir naming convention.
  - **CHANGELOG.md `[Unreleased]` § Added** entry — documents the shipped contract, the detector classes, the false-positive disciplines, the critical-flag heuristic, the closest-match suggestion path, the CLI entry-point default, the skill-local-first posture, and the zero-touch contract on `anvil/lib/`. The entry is positioned before the Phase 1 (#333) entry to follow the chronological reverse order.
  - **Scope discipline preserved (the load-bearing constraint).** Zero `anvil/lib/` changes. Zero schema changes in `anvil/lib/review_schema.py`. Zero changes to `anvil/lib/cite.py` (read-only consumer per the issue body's explicit out-of-scope list). Zero changes to `anvil/skills/memo/rubric.md` (Phase 1 just edited dim 3 weighting; the critical-flag short-circuit already exists). Zero changes to `anvil/skills/memo/commands/memo-review.md` (the 4f / 4g / 4h / 4i step structure renumbered by PR #332 is load-bearing). Memo-first per the skill-local-first rule — no changes to pub / report / proposal / deck / slides / ip-uspto / installation / project-migrate.
  - **Critical-flag threshold decision.** The `>5 unhooked claims OR any named-author unhooked` heuristic is documented in both the module docstring (`citation_coverage.py` §"Critical-flag heuristic") and the command doc (`memo-citations.md` §"Critical-flag heuristic"). The threshold is chosen so the rubric's dim 3 reviewer keeps its headroom for the borderline (1–5 unhooked) case while a citation-light memo (>5) trips the critical pathway. The named-author short-circuit reflects that named-author claims are the highest-confidence positive class (the issue body's "intrinsically load-bearing" framing) — an unhooked one is a fabrication risk and should fire the critical pathway even on a single instance. The choice is conservative on the positive side (the >5 threshold lets normal-density memos clear without the critical flag) and aggressive on the high-confidence side (named-author intrinsic load-bearing). Tunable as a module-level constant (`CRITICAL_UNHOOKED_THRESHOLD`) so a future canary signal can adjust without touching the detector logic.
  - **Coordination with Phase 2 (#335 `hyperlink_resolver`).** The two Track B critics share the same general shape (deterministic detector → `tool_evidence`-kind `_review.json` → sibling critic dir). Issue #336 was claimed while #335 was still in flight; the CLI entry-point and sibling-dir naming conventions were the two deferred decisions called out in the issue body's coordination section. Picked the documented default — `python -m anvil.skills.memo.lib.<module>` for the CLI and `<version_dir>.citations/` for the sibling — and documented the choice in both the module docstring and the command doc. If Phase 2 lands first with a different runner shape (e.g. an `argparse` subcommand or a different output layout), the `_cli_main` function in `citation_coverage.py` is the one place to mirror it. The convergence is post-merge editorial work; both critics work as-is in their current shapes.
  - **Touched**: NEW `anvil/skills/memo/lib/citation_coverage.py`, NEW `anvil/skills/memo/commands/memo-citations.md`, NEW `tests/skills/memo/test_citation_coverage.py`, MODIFIED `CHANGELOG.md` [Unreleased] entry, MODIFIED this `WORK_LOG.md` entry. No changes to `anvil/lib/`, no schema changes, no other-skill changes.
  - **Test results**: full suite green — 1239 passed, 2 skipped (network smoke tests under `tests/lib/`). The 46 new tests in `tests/skills/memo/test_citation_coverage.py` cover all four detector classes (positive + false-positive), both citation styles (broken + resolved), the closest-match suggestion, the critical-flag heuristic, the filesystem integration, the auto-discovery contract, and the CLI entry-point smoke.

- **Issue #335 (Epic #328 Track B Phase 2)**: shipped the `hyperlink-resolver` critic for the memo skill — a new deterministic tool-evidence detector that walks every link expression in a memo version directory and emits a canonical `_review.json` for broken cross-thread refs, broken markdown internal paths, broken wiki-links, and (behind the off-by-default `--check-external` flag) failing external HTTP/HTTPS links. The critic is the second mechanical detector in the deterministic-checks family (alongside `anvil/lib/render_gate.py` and `anvil/lib/revise_consistency.py`) and the first active Track B deliverable of the reframed Epic #328 (Track A judgment-enrichment shipped via the memo rubric in #333 / PR #334; Track B Phase 3 ships the citation-coverage critic in parallel issue #336).
  - **`anvil/skills/memo/lib/hyperlink_resolver.py`** is the new skill-local lib module (~720 lines including docstrings). Four link classes with severity / verdict mapping: (1) cross-thread refs `[[../slug/slug.N]]` — delegates to `cross_thread_refs.resolve_cross_thread_ref` (no duplicate parsing per the AC; the existing typed resolver covers ~60% of the surface), severity `blocker` on failure AND raises the load-bearing `critical_broken_cross_thread_anchor` flag so the aggregator's `compute_verdict` short-circuits to `Verdict.BLOCK`; (2) markdown internal links `[text](path)` — file-existence check against the version dir (trailing `#anchor` / `?query` stripped; anchor validity is out of scope per the issue body), severity `major` on missing target; (3) markdown external links `[text](https://...)` — recorded but NOT probed by default (offline-safe / CI-reproducible), probed via subprocess `curl -I` with a 5-second timeout when `--check-external` is set, severity `major` on 4xx / 5xx / timeout; graceful-degrades when `curl` is absent from PATH (top-level reason, no false-positive findings); (4) wiki-links `[[document-name]]` — looked up against the enclosing project's `BRIEF.md` `documents:` list (resolved via `project_discovery.discover_thread_root` + `project_brief.load_project_brief`), severity `major` on unknown slug or missing BRIEF. Output: canonical `Review` per `anvil/lib/review_schema.py` with `kind=tool_evidence`, `critic_id=hyperlinks`, single null-scored row for `dimension=hyperlinks` (the critic owns no rubric dim; feeds verdict via critical-flag short-circuit and as evidence the reviewer consumes alongside its dim 3 scoring), one `Finding` per broken link with `tool_calls=[]` to satisfy the schema validator, and one `CriticalFlag` of type `critical_broken_cross_thread_anchor` when any cross-thread ref failed (empty otherwise).
  - **`anvil/skills/memo/commands/memo-hyperlinks.md`** is the new command doc (mirrors the structural shape of an existing tool-evidence command — frontmatter, inputs / outputs sections, procedure, link-classes table, CLI entry-point, auto-discovery wiring section, failure modes table, idempotence + what-the-critic-does-NOT-do paragraphs). Documents the agreed coordination points with the parallel Phase 3 citation-coverage critic (#336): CLI entry-point convention `python -m anvil.skills.memo.lib.<module> <version_dir> [--check-external] [--write-review]` and output-dir naming `<version_dir>.<critic-tag>/` (`.hyperlinks/` here; `.citations/` for #336). Both match the `<version_dir>.<tag>/` pattern that `anvil/lib/critics.py::discover_critics` recognizes without code changes — no aggregator changes needed; the second-consumer of the convention is exercised in this PR.
  - **`tests/skills/memo/test_hyperlink_resolver.py`** ships 28 new tests covering every issue #335 acceptance criterion: intact cross-thread ref produces no findings; broken cross-thread ref (missing version / missing sibling thread) emits `blocker` + `critical_broken_cross_thread_anchor`; broken markdown internal link emits `major`; markdown external link with `--check-external` OFF completes without network access (asserted via a `subprocess.run` mock that raises if invoked); markdown external link with `--check-external` ON returns `major` on mocked 404 / 500 and no finding on mocked 200; wiki-link `[[unknown-doc]]` emits `major` referencing BRIEF.md; auto-discovery via `discover_critics` + `load_review` + `aggregate` round-trip producing `Verdict.BLOCK` on a broken cross-thread anchor; CLI entry-point exit codes (0 clean / 1 findings / 2 invocation error) plus `--write-review` producing the canonical sibling dir; doc-coverage suite over the new command file (frontmatter, `--check-external` documentation, critical-flag name, `.hyperlinks/` convention). Adds two schema-discipline regression guards: one asserts the emitted `Review` round-trips through `Review.model_validate` with `tool_calls=[]` on every `Finding` (the schema validator requires it for `kind=tool_evidence`); one asserts the `Finding` model has NOT grown the deferred-experiment fields (`action` / `target_anchor` / `proposed_content`) so the Phase 2 no-schema-delta settle remains enforced at test time.
  - **Touched**: ADDED `anvil/skills/memo/lib/hyperlink_resolver.py` (~720 lines), ADDED `anvil/skills/memo/commands/memo-hyperlinks.md` (~120 lines), ADDED `tests/skills/memo/test_hyperlink_resolver.py` (28 new tests, ~480 lines), MODIFIED `CHANGELOG.md` [Unreleased] entry, MODIFIED this `WORK_LOG.md` entry. No `anvil/lib/` changes (skill-local first per CLAUDE.md), no `anvil/lib/review_schema.py` changes (no schema delta — settled at Epic #328 kickoff), no `anvil/skills/memo/rubric.md` changes (Phase 1's dim-3 enrichment guidance from #333 / PR #334 is left alone — the Phase 1 + Phase 2 contracts are complementary, not coupled). The `memo-review.md` step structure (steps 4f / 4g / 4h / 4i) is unchanged per the out-of-scope constraint.
  - **Reuses primitives, does not reimplement**: cross-thread ref parsing delegates to `cross_thread_refs.find_cross_thread_refs` + `cross_thread_refs.resolve_cross_thread_ref` (per the AC); wiki-link BRIEF lookup uses `project_discovery.discover_thread_root` + `project_brief.load_project_brief` (lenient loaders so a missing BRIEF degrades to `"BRIEF.md not found"` per-finding rather than raising); the `Review` / `Finding` / `CriticalFlag` shape follows the existing `tool_evidence` precedents in `anvil/lib/render_gate.py` (`GateResult.to_review`) and `anvil/lib/revise_consistency.py` (`ConsistencyResult.to_review`) — single null-scored row + one Finding per failure + `tool_calls=[]` to satisfy the schema validator.
  - **External HTTP discipline: off by default.** The `--check-external` flag is opt-in so the critic stays offline-safe and CI-reproducible (no flaky tests gated on network availability; no surprise outbound HTTP on a developer machine). External links are still recorded with `resolved=True` and a reason string when the flag is off so the reviewer can see at a glance which external sources the memo cites; the resolver does not synthesize findings on links it didn't probe.
  - **Critical-flag pathway: the load-bearing surface.** The four link classes are valuable but the cross-thread-anchor case is the one that forces `Verdict.BLOCK` regardless of dim-by-dim scoring. The issue body explicitly named this as the AC: a memo whose evidence chain points at a missing sibling version is structurally broken. The `critical_broken_cross_thread_anchor` flag deduplicates by `(type, justification)` in the aggregator (per `_dedupe_critical_flags`), so multiple broken cross-thread refs in the same memo produce a single critical-flag entry with a summary justification listing the first three failures by `(raw, reason)` pair.
  - **Coordination comment on #336**: posted to the parallel Phase 3 builder so the citation-coverage critic can mirror the CLI entry-point shape (`python -m anvil.skills.memo.lib.citation_coverage <version_dir>`) and output-dir naming (`<version_dir>.citations/`). No blocking dependency between #335 and #336 — both can land independently; the conventions are documented for consistency, not enforcement.
  - **Per-skill test filename uniqueness (#58)**: `test_hyperlink_resolver.py` is unique across the `tests/skills/*/` tree (verified by inspection). The file lives under `tests/skills/memo/` per the cross-skill test-location convention; an in-skill test dir (`anvil/skills/memo/tests/`) was not used because the AC asked for `tests/skills/memo/test_hyperlink_resolver.py` specifically.

- **Issue #333 (Epic #328 Track A Phase 1)**: extended `anvil/skills/memo/rubric.md` with a new §"Figure + hyperlink enrichment (dim 3, advisory `scope: expand`)" subsection inside §"Refs back-check (dim 3)" that instructs the standard `memo-review` reviewer to emit `scope: expand` enrichment findings on four classes of gap — missing or off-brand figures (load-bearing claims where a chart / table / diagram would clarify; figures that visibly diverge from the brand palette in `anvil/lib/figures/palette.py`), missing or inadequate alt text on markdown `![alt](path)` and HTML `<img>` references (empty alt, literal `image` / `figure` / `chart` placeholders, sub-10-character non-descriptive alt — the existing `memo_image_refs_exist` lint covers existence only; this enrichment closes the alt-text-quality gap), prose references to figures that do not exist in the version directory (`see Figure N` / `as shown in Chart M` / `Table K reports` patterns where the referenced anchor is not present), and suggested-but-missing hyperlinks on load-bearing claims (advisory, lower priority than the first three). Phase A reviewer-prose-only — no schema delta, no new critic-sibling type, no new command, no Python detector. Mirrors the structural shape of the strongman back-check (#330 / PR #332 — merged earlier today) with subsection headings, severity ladder, calibration notes (scoring posture, backwards compatibility, Phase A / Phase B split). The change is the first active phase of the reframed Epic #328 (Track A — judgment enrichment via rubric guidance, zero schema cost) — Phases 2 (`hyperlink-resolver`) and 3 (`citation-coverage`) are Track B mechanical detectors that are informed by Phase 1's outcome but do not block on it.
  - **`anvil/skills/memo/rubric.md`** extended §"Refs back-check (dim 3)" with the new §"Figure + hyperlink enrichment (dim 3, advisory `scope: expand`)" subsection (positioned after the §"Strongman back-check (dim 3)" subsection that landed in PR #332 earlier today, before §"Summary-detail consistency"). Subsection structure mirrors the strongman back-check: opening paragraph naming the four enrichment classes and the canary lineage (2026-06-05 studio `.enrich/` brains-for-robots hack); §"Authoring-surface decision (precedent)" sub-heading documenting the `rubric.md` vs `rubric_overrides` choice; §"Enrichment classes" with the four numbered scopes (each naming the sub-cases, the specific instance the reviewer cites, and the concrete additions proposed — e.g., for off-brand figures, points at `anvil/lib/figures/palette.py` constants `ANVIL_NAVY = "#1f4e7a"` / `ANVIL_INK = "#1a1a1a"` / `ANVIL_MUTED = "#6b6b6b"` and the `anvil.mplstyle` regeneration path); §"Severity ladder" as a table mapping the six (sub-class × class) combinations to `major` / `minor` / `nit` and confirming `comments.md` shape + dim 3 impact (none — enrichment-only); §"Scoring posture" documenting why enrichment is `comments.md`-side only and does NOT alter the dim 3 /6 score; §"Backwards compatibility" documenting the always-active behavior and the per-thread `rubric_overrides.dim_3_calibration` opt-out path; §"Phase A / Phase B split" naming the Phase A reviewer-prose-only contract and the Phase B Track B detectors (`hyperlink-resolver`, `citation-coverage`) gated on canary signal. The §"Phase A / Phase B split" subsection also documents the validation step — when the canary author's `.enrich/` brains-for-robots fixtures are attached to #333 (offered in #328's comment thread but not yet attached), they serve as the Phase B regression anchor in the same way `tests/fixtures/summary_detail_consistency/raytheon_gen_attribution/` anchors the summary-detail Phase B detector.
  - **Authoring-surface precedent codified.** The enrichment guidance lives in `rubric.md` (per-skill, durable) rather than in `rubric_overrides` on `BRIEF.md` (per-thread, ephemeral) because the four enrichment classes are **always-on cross-thread guidance** the team wants firing on every memo unless explicitly scoped out. Per-thread tuning (e.g., "this memo is a synthesis brief; figures are intentionally minimal") lives as a `rubric_overrides.dim_3_calibration` calibration suffix on the matching BRIEF.md document entry — it attaches to dim 3's `scoring.md` justification via the existing Reader-dispatch-order contract (memo-review step 4h) and the reviewer respects it inline (no false-positive figure-enrichment findings on a prose-only thread). The decision is load-bearing for future enrichment scopes: image-accessibility deeper checks (alt-text generation, contrast checks), claim-grounding (prose claim → external evidence anchor), and any other enrichment scope landing as Track A guidance will follow the same precedent — `rubric.md` for cross-thread always-on, `rubric_overrides` for per-thread tuning.
  - **`commands/memo-review.md` step structure is unchanged.** Per the issue body's out-of-scope constraint, steps 4f / 4g / 4h / 4i (just renumbered by PR #332's strongman back-check addition earlier today) are left alone. The new enrichment guidance attaches to existing step 5's dim 3 scoring loop via the `comments.md` write at step 8, NOT via a new step 4-prefix sub-step. The reviewer's per-dim scoring loop already drives the `comments.md` write at step 8; the new sub-rule expands the **content** of that write (which `scope: expand` enrichments get emitted) without altering the **structure** of when the write happens. This preserves the four-leg back-check structure documented in step 4e / 4f / 4g (summary-detail, cross-thread, strongman) — those legs all emit their own top-level `_summary.md` blocks; the enrichment guidance here does NOT introduce a new `_summary.md` block (the findings flow through `comments.md` directly, no structured-block intermediate needed because there is no critical-flag pathway to wire — see §"Scoring posture").
  - **Touched**: MODIFIED `anvil/skills/memo/rubric.md` (single insertion of the new subsection — ~130 lines of rubric prose), MODIFIED `CHANGELOG.md` [Unreleased] entry, MODIFIED this `WORK_LOG.md` entry. No code changes; no `anvil/lib/` changes; no `anvil/lib/review_schema.py` changes; no new Python detector; no new critic-sibling type; no new command. Tests under `tests/skills/memo/` pass byte-identically because the change is additive prose inside an existing rubric section the doc-coverage tests do not pin verbatim.
  - **Phase A discipline preserved (the load-bearing scope constraint).** Same shape as the strongman back-check (#330 / PR #332) — zero `anvil/lib/` changes, zero schema delta, zero new Python detector, zero new critic-sibling type, zero new command, documentation-only inside `anvil/skills/memo/rubric.md`. Follows the existing back-check Phase A precedent ladder: §"Refs back-check (dim 3)" (PR #144 / PR #140), §"Summary-detail consistency" (PR #250 / #245), §"Cross-thread citation back-check" (#236), §"Strongman back-check" (#330 / PR #332), §"Figure + hyperlink enrichment" (this issue, #333).
  - **Scoring posture: enrichment-only.** The sub-rule does NOT alter the dim 3 /6 score. The existing dim 3 sub-rules (citation hooks, refs back-check, strongman back-check, cross-thread cite back-check) own the per-instance deduction surface; this layer is a `comments.md`-side observation that proposes additions for the next revision to consider. The reviser at the next pass MAY decline any enrichment finding via the `scope: expand` decline pathway (no penalty); the reviewer at the next version SHOULD NOT re-fire the same finding when the prior reviser declined it with a documented rationale. The decision to keep enrichment OFF the dim 3 score (rather than emitting deductions) is load-bearing for the canary's "judgment enrichment is enough" hypothesis: the studio's `.enrich/` hack ran without altering the existing /44 score, and the canary signal showed that the `comments.md` enrichment stream — independent of score — was the operationally useful surface. Track B's mechanical detectors (`hyperlink-resolver` and `citation-coverage`, Epic #328 Phases 2–3) MAY introduce score deductions when they ship; Track A intentionally does not.
  - **Backwards compatibility: always-active.** Unlike the strongman / refs / cross-thread back-checks (which require author-supplied on-disk substrate to fire), the figure/hyperlink enrichment sub-rule is always active — no substrate trigger needed. A memo with no figures, no image refs, and no figure-reference prose simply produces zero enrichment findings of classes 1–3 (and likely zero of class 4 if the citation-hook contract is fully satisfied). A reviewer running on a memo authored before this sub-rule shipped behaves byte-identically — there are no missing-figure findings to emit when no claim load-bearingly anticipates a figure. A per-thread `rubric_overrides.dim_3_calibration` that scopes out figure enrichment attaches as a calibration suffix and the reviewer respects it inline.
  - **Validation step: fixture-request follow-on.** The canary author's `.enrich/` brains-for-robots fixtures were offered in Epic #328's comment thread (2026-06-05) as `<thread>.{N}.enrich/` critic siblings with `comments.md` / `verdict.md` / `_meta.json` / `_progress.json` files demonstrating the judgment-enrichment output quality on three parallel subagents (figures / images / hyperlinks) per document across 8 brains-for-robots threads. The fixtures are not yet attached to issue #333 — they serve as the Phase B regression anchor when attached. A fixture-request comment may be filed on #333 to surface the request to the canary author. The rubric guidance ships first; fixture validation is a follow-on and does not block landing per the issue body's acceptance criteria.
  - **Regression risk (open question 2 from #333 / open question 4 from #328).** Does loading the existing reviewer with figure/hyperlink enrichment scope dilute its judgment quality on existing dimensions? The canary's `.enrich/` hack ran the enrichment as **parallel subagents** (three per document) rather than as additional scope on the existing reviewer — so the canary signal does not directly answer this question. The Phase A landing here merges the enrichment scope into the standard `memo-review` reviewer; the next canary cycle (the studio re-running review on the same brains-for-robots threads with the updated rubric) is the natural validation step. If the canary signal shows degradation on the existing dimensions, the sub-rule can be promoted to a `rubric_overrides` opt-in by the consumer (with the rubric guidance kept as opt-in documentation), or the enrichment can be moved to a sibling `<thread>.{N}.review-enrich/` critic dir that runs as a second pass after the standard review.
  - **Future Track B promotion gated on canary signal.** Phases 2 (`hyperlink-resolver`) and 3 (`citation-coverage`) of Epic #328 are Track B mechanical detectors that ship as `tool_evidence`-kind critic siblings — `hyperlink-resolver` wraps the existing `anvil/skills/memo/lib/cross_thread_refs.py` and adds external HTTP HEAD checks behind a `--check-external` flag; `citation-coverage` builds on `anvil/lib/cite.py` + `anvil/skills/memo/lib/refs_resolver.py` to detect unhooked load-bearing claims and broken `\cite{}` / `[@]` refs. They are deliberately NOT filed yet — they are informed by this sub-rule's canary validation outcome. Once a future canary cycle confirms (a) the enrichment scope does not degrade reviewer judgment quality on existing dimensions, AND (b) the `comments.md` enrichment output quality matches the `.enrich/` hack, the Phase 2 / Phase 3 child issues can land. The 4-week canary observation window precedent from PR #205 / PR #215 / PR #317 applies.

- **Issue #330**: standardized the studio canary's strongman practice (paired adversarial briefs scoped per research question, stored in the research/input layer alongside other source-of-truth materials) as recognized source-of-truth materials in `anvil:memo`. Documentation-only convention formalization — no new critic sibling type, no schema changes in `anvil/lib/review_schema.py`, no new Python detector, no new command. The pattern was surfaced by three studio threads independently reaching for the same file shape (`brasidas-licensing` with 5 vertical pairs in `research/<vertical>-analysis/`, `brains-for-robots/investment-memo` with 7 vertical pairs, `brains-for-robots/broadcom-thesis` with one pair on the Broadcom-Sphere analogy); the second-consumer threshold for the pattern shape was reached months ago and three active consumers cleared the bar comfortably. Four files touched inside `anvil/skills/memo/` per the curator's recommended edit order (so cross-refs resolve as the edits land): SKILL.md → rubric.md → memo-review.md → memo-draft.md.
  - **`anvil/skills/memo/SKILL.md`** extended §"Source-of-truth materials" (line 63 area) with two new recognized filenames — `strongman-for.md` (best-possible case for a named thesis or research question; advocacy-shaped; dim 2 *Thesis coherence* calibration substrate) and `strongman-against.md` (hardest-possible case against; adversarial-shaped; dim 3 *Evidence quality* back-check substrate) — plus a follow-up §"Strongman scoping convention" paragraph documenting the per-thesis scoping rule (one named thesis or research question, NOT the memo artifact as a whole; multiple pairs may exist per thread root, typically organized into `refs/<topic>/` for single-thesis memos or `<portfolio>/research/<topic>-analysis/` for portfolio-level multi-vertical memos). The convention explicitly notes that strongman files live in the research/input layer, NOT as a critic sibling of a versioned memo dir — they are author-supplied substrate, not critique output. Updated the disambiguation rule's example list to include strongman filenames alongside the existing source-of-truth content-named shapes.
  - **`anvil/skills/memo/rubric.md`** extended §"Refs back-check (dim 3)" (line 122 area) with a new §"Strongman back-check (dim 3)" subsection documenting the three-valued classifier (`ADDRESSED` / `PARTIALLY_ADDRESSED` / `NOT_ADDRESSED`) and the load-bearing-aware severity ladder. The severity mapping splits on the load-bearing-ness of the objection: `NOT_ADDRESSED` on a load-bearing objection (one the strongman author marked as thesis-defining or recommendation-shifting, or one a sophisticated reader would identify as a deal-breaker) surfaces as `critical` severity with a -2 dim 3 deduction AND a critical-flag candidate; `NOT_ADDRESSED` on a non-load-bearing objection (peripheral or speculative concerns the strongman author included for completeness) surfaces as `important` severity with -1 dim 3 deduction and is observational only; `PARTIALLY_ADDRESSED` (memo touches the objection but does not fully engage) surfaces as `important` severity with -1 dim 3 deduction. `strongman-for.md` calibrates dim 2 (Thesis coherence) — the reviewer's dim 2 justification SHOULD note whether the memo's thesis aligns with the strongest version of its own argument when `strongman-for.md` is present. The three sub-rules under §"Refs back-check (dim 3)" (citation hooks, refs back-check proper, strongman back-check) are independent and additive: a memo can lose points on all three. Backward compatibility preserved — a memo with no strongman files is unaffected.
  - **`anvil/skills/memo/commands/memo-review.md`** added new sub-step 4g (strongman back-check, Phase A reviewer-prose-only) implementing the per-objection classifier with the three-phase procedure: (1) enumerate strongman files in the resolved refs-dir list (recursing one level deep to catch `refs/<topic>/strongman-{for,against}.md` and `research/<topic>-analysis/strongman-{for,against}.md`), (2) enumerate the named load-bearing objections inside each `strongman-against.md` (the strongman author named them; the reviewer is NOT re-deriving them — the file's structure is the contract), (3) classify each objection per the `ADDRESSED` / `PARTIALLY_ADDRESSED` / `NOT_ADDRESSED` ladder with load-bearing-aware severity. Renumbered the prior 4g (rubric_overrides loader from issue #233/#296) to 4h and the prior 4h (artifact-type overlay from issue #286) to 4i; updated four internal cross-references that pointed at the rubric_overrides step. The new step mirrors the structural shape of step 4f (cross-thread cite back-check) with a 3-valued vocabulary instead of 4-valued. Added the `strongman_back_check` top-level block to the `_summary.md` schema (sibling to the existing `summary_detail_consistency`, `cross_thread_cite_consistency`, `scope_distribution`, and `rubric_overrides` top-level blocks, NOT nested under `lint` — same rationale as the prior judgment-shaped blocks: the `lint` namespace is reserved for deterministic mechanical checks) carrying `ran`, `strongman_against_files_scanned`, `strongman_for_files_scanned`, `objections_enumerated`, `findings_count`, `findings_by_severity`, `findings` (per-finding fields: `objection_id`, `objection_title`, `strongman_source`, `load_bearing`, `verdict`, `severity`, `dim_3_deduction`, `justification`), and `critical_flag_candidate`. Added the `## Strongman back-check findings` subsection to `findings.md` (always emitted; three shapes — findings present / skipped with reason / clean run). Extended step 7 (verdict aggregation) with the strongman critical flag, step 10 (verdict.md write) with the `Strongman: NOT_ADDRESSED (load-bearing)` critical-flag entry and the strongman-driven top-3 revision priority ("Address or explicitly scope out the load-bearing objection from `strongman-against.md`").
  - **`anvil/skills/memo/commands/memo-draft.md`** extended step 3 *Read inputs* (line 37 area) with the §"Strongman drafter contract" sub-paragraph documenting that when strongman files are present in any resolved refs-dir (per-thread `refs/`, per-thread `refs/<topic>/`, or portfolio-level `research/<topic>-analysis/`) the drafter: (a) reads them as authoritative load-bearing substrate with the same precedence rule as other source-of-truth materials (strongman wins on contradiction; the drafter MUST rewrite the claim to agree or flag the conflict explicitly); (b) for each `strongman-against.md`, identifies the named load-bearing objections inside it and ensures the produced draft either directly addresses each in prose with reasoning that engages the objection on its merits OR explicitly scopes the objection out of the memo's claim set; (c) does NOT simply ignore the file (the reviewer at memo-review step 4g will check); (d) for each `strongman-for.md`, reads the file as load-bearing context for the thesis statement and ensures alignment with the strongest framing for dim 2. Extended step 6 *Evidence* §"Source-of-truth refs as authoritative hooks" to list strongman files among the canonical source-of-truth shapes.
  - Touched: MODIFIED `anvil/skills/memo/SKILL.md`, MODIFIED `anvil/skills/memo/rubric.md`, MODIFIED `anvil/skills/memo/commands/memo-review.md`, MODIFIED `anvil/skills/memo/commands/memo-draft.md`, plus this WORK_LOG entry and the [Unreleased] CHANGELOG entry. No code changes, no new tests required (the existing `tests/skills/memo/test_memo_refs_back_check_doc.py` substring-presence tests pass byte-identically because the SKILL.md additions are additive — the existing "Source-of-truth materials" subsection and "Citation stubs" subsection and the canonical filename names are all preserved verbatim).
  - **Phase A discipline preserved (the load-bearing scope constraint).** Zero `anvil/lib/` changes. Zero schema changes in `anvil/lib/review_schema.py`. Zero new Python detector (the back-check is reviewer-prose-only — same shape as the §"Refs back-check (dim 3)" precedent at PR #140 / PR #162, the §"Summary-detail consistency" precedent at PR #250 / issue #245, and the §"Cross-thread citation back-check" precedent at issue #236). Zero new critic-sibling type (strongman files are author-supplied substrate, NOT critique output; they live in the research/input layer alongside CVs, transcripts, filings — not as siblings of versioned memo dirs). Zero new command. The entire change is documentation in four files inside `anvil/skills/memo/`.
  - **Three-valued vocabulary, deliberate divergence from the existing 4-valued back-check vocabularies.** The strongman back-check ships with `ADDRESSED` / `PARTIALLY_ADDRESSED` / `NOT_ADDRESSED` (3-valued) while the §"Refs back-check (dim 3)" uses `VERIFIED` / `UNVERIFIED` / `CONTRADICTED` / `NOT-IN-REFS` (4-valued) and the §"Cross-thread citation back-check" uses `ANCHOR-FOUND` / `ANCHOR-MISSING-BUT-THREAD-PRESENT` / `ANCHOR-CONTRADICTED` / `THREAD-NOT-FOUND` (4-valued). The divergence is **load-bearing**: the strongman back-check classifies the *memo's treatment of an author-named objection*, not the *truth state of a claim against a source* — the natural vocabulary is "did the memo engage the objection?" (addressed) / "partially?" (partially_addressed) / "no" (not_addressed), not the "verified/contradicted/missing" shape of an evidence back-check. Each back-check leg's vocabulary is canon for that leg; implementers SHOULD NOT normalize across the four legs (refs / summary-detail / cross-thread / strongman). Same divergence-as-signal precedent set by the `lint.*` (`error` / `warning` / `info`) vs reviewer-judgment (`critical` / `important` / `suggestion`) split.
  - **Substrate-driven fourth leg of the back-check structure.** The framework's existing back-check triangle (memo A claim ↔ memo A `refs/` + memo A summary ↔ memo A §N + memo A claim ↔ memo B §N — three legs all anchored to the memo's own claims and prior anvil substrate) gains a substrate-driven fourth leg: memo A claim-set ↔ author-named objections in `refs/strongman-against.md`. This closes the gap between "the memo addresses the evidence on hand" (the triangle) and "the memo addresses the strongest counter-arguments the author or operator has identified" (this leg). The triangle framing is preserved verbatim in the existing rubric.md and memo-review.md cross-references; the strongman leg is documented as a co-equal sub-rule under §"Refs back-check (dim 3)" rather than as a structural fourth leg of the triangle — the triangle metaphor stays canonical because the three original legs share the "memo claim ↔ on-disk anchor" shape, while the strongman leg has the inverse shape ("on-disk objection ↔ memo treatment").
  - **Future Phase B promotion gated on canary signal.** A Phase B automated detector (e.g., `anvil/skills/memo/lib/strongman_back_check.py`) is the documented follow-on once: (1) the existing three-consumer canary surface (brasidas-licensing, brains-for-robots/investment-memo, brains-for-robots/broadcom-thesis) generates enough false-positive vs true-positive signal to anchor the detector's heuristic surface; (2) a second skill (deck, proposal, pub) adopts the strongman pattern, satisfying the CLAUDE.md "wait for the second consumer before generalizing" lib-promotion contract. v0 ships at the rubric-prose discipline level only — the same shape as PR #144 / PR #250 / issue #236 followed and the same shape PR #205 / PR #215 followed before the parity-lint promotion in PR #317 fired.

- **Issue #319**: fixed the redundant `<link rel="stylesheet" href="styles.css" />` line in `anvil/lib/memo/template.html` that produced a weasyprint stderr `ERROR: Failed to load stylesheet` on every `anvil:memo-render` run on consumer installs. **Root cause**: the relative `href` was resolved by weasyprint against pandoc's CWD (the consumer repo root, not the `anvil/lib/memo/` directory the template lives in), so the fetch failed against a non-existent `/Users/<...>/<consumer-repo>/styles.css` path. The render succeeded and the styling landed because `anvil/lib/render_gate.py:962-963` separately passes the absolute `--css <abs path to anvil/lib/memo/styles.css>` on the weasyprint and wkhtmltopdf branches — the `<link>` tag was strictly redundant. **One-line fix**: deleted `anvil/lib/memo/template.html:36`. Chose **Option A** from the issue body (remove the `<link>`) over Option B (`$css$` template variable with pandoc-resolved absolute path) because A matches the existing `--css` invariant in `render_gate.py` — Option B would force pandoc to thread the resolved path through metadata, adding a moving part with no observable benefit. **Test impact**: inverted the existing `assert "styles.css" in text` line at `tests/lib/test_memo_render_detection.py:309` to a deletion-and-replace shape: the surviving `test_lib_memo_ships_pandoc_html_template` no longer asserts that string and gains a docstring explaining the `--css` delivery channel, plus a NEW regression guard `test_lib_memo_template_html_avoids_bare_stylesheet_link` that asserts the `<link rel="stylesheet">` shape is absent from `template.html` (regex-based, strips HTML comments first so the doc block at the top of the template — which legitimately documents the pandoc `--css` invocation — does not trip the guard). The new guard is modeled on `tests/skills/memo/test_memo_styles_page_size_doc.py::test_styles_css_page_block_avoids_bare_letter_keyword`, the canonical "this shape must NOT appear" inverse-assertion pattern in the memo-side test surface. Touched: MODIFIED `anvil/lib/memo/template.html` (line 36 deleted), MODIFIED `tests/lib/test_memo_render_detection.py` (one assertion removed, docstring extended, NEW regression-guard test added, module-level `re` import added), MODIFIED `CHANGELOG.md` `[Unreleased]` `### Fixed` entry, plus this WORK_LOG entry. No new Python deps; subprocess-only contract preserved.
  - **Severity at report time**: cosmetic. The render succeeded; the styling landed (verified by canary against `output/clear-signal/memo/memo.1/`). The reporter's "promotes to blocker" call is correct — `--fail-if-warnings` is already on at `render_gate.py:972`, so this is one weasyprint-version-or-flag tightening away from a hard render failure. The fix lands the styling via the existing `--css` invariant and removes the stderr warning surface entirely.
  - **Out of scope (deliberately)**: `anvil/lib/render_gate.py` — the `--css` invariant is correct and load-bearing; the xelatex branch (`template.tex`) — the LaTeX path has no `<link>` and reads pandoc's `--variable=papersize` chain, unaffected; `anvil/lib/memo/styles.css` itself — the stylesheet content is correct, only its delivery channel needed simplification. `anvil/lib/memo/README.md:31` cosmetically still mentions `template.html` "referencing `styles.css`" — the wording is stale post-fix but not load-bearing (the chain diagram at lines 41-47 correctly shows `pandoc --template template.html --css styles.css → memo.html` which remains the actual invocation) and was flagged optional by the curator; left as a follow-on micro-polish if a reader trips on it. `scripts/install-anvil.sh:651,657` lists `template.html` as a hand-edit override site — consumers who hand-edited a `<link>` back in get the original CWD-relative behavior in their fork, which is their problem.
  - **Verification**: the targeted suite (`tests/lib/test_memo_render_detection.py` + `tests/skills/memo/test_memo_styles_page_size_doc.py`) is green at 25/25 including the new regression guard. The full pytest suite remains green. End-to-end weasyprint reproduction against the canary's `output/clear-signal/memo/memo.1/` is a manual smoke step recommended by the curator (the framework test suite does not end-to-end render); the change is mechanical and the existing `tests/skills/memo/test_memo_render_*` doc-coverage tests pin the `--css` invocation shape unchanged.

- **Issue #317** (PR #205 follow-on #2 of 4): promoted the two skill-local `parity_lint.py` mirrors into a single shared `anvil/lib/parity.py` per CLAUDE.md §"Skill-local first, lib promotion later". The second-consumer trigger fired on merge of PR #215 (memo-side mirror — see 2026-06-01 entry: "Second-consumer trigger fires with this PR's merge"); this PR is the canonical one-line import-path swap both skill-local docstrings had already named as the target. NEW `anvil/lib/parity.py` carries the byte-identical core (`EXTRACTORS`, `Finding`, `LintResult`, `UNIT_VOCABULARY`, `_LINT_DISABLE_RE`, `_normalize_token`, `_extract_tokens`, `_collect_disabled_tokens`) plus a unified `lint_source(deck_source, memo_source, *, rule=...)` core, both legacy file wrappers (`lint_deck_memo_parity`, `lint_memo_deck_parity`), AND a new `lint_parity(primary_path, sibling_path, primary_kind, sibling_kind) -> LintResult` unified wrapper that dispatches on the (primary_kind, sibling_kind) pair. `RULES_DECK` / `RULES_MEMO` preserve the per-side rule labels (`deck_memo_parity` / `memo_deck_parity`) verbatim because those names are grep-pinned by the two `*-review.md` command files. Both skill-local modules (`anvil/skills/deck/lib/parity_lint.py`, `anvil/skills/memo/lib/parity_lint.py`) shrink to ~35-line thin re-exports; the memo-side re-export wraps `lint_source` to preserve its `(memo_source, deck_source)` positional-arg order (the only meaningful asymmetry between the two pre-promotion modules — exploited by `test_symmetry_with_deck_side_lint_source`). NEW `tests/lib/test_parity.py` (21 tests) covers the shared module directly: extractor surface, per-side rule labels, both `Finding` / `LintResult` field shapes (incl. both `memo_sibling` and `deck_sibling` fields coexisting on every result), citation-clear canary regression in both directions, both canary-message verbatim wordings (`"memo.4 introduced a"` deck-side; `"the symmetric direction"` memo-side), escape-hatch with rule-label scoping, symmetry contract, unified-wrapper dispatch on the (deck, memo) and (memo, deck) pairs plus `ValueError` on unsupported same-kind pairs, Phase A severity contract, and a `is`-identity smoke test confirming the skill-local re-exports point at the same shared objects. The 58 existing skill-local tests pass unchanged modulo four docstring-content tests (one on the deck side, three on the memo side) that were authored against the pre-promotion docstrings — those four tests now read from `anvil.lib.parity.__doc__` instead, preserving the test intent (the promotion narrative remains discoverable) with only the import path changed. Command docs (`deck-review.md` step 5d, `memo-review.md` step 4d) are byte-identical pre/post (grep-pinned by the doc-coverage tests). Touched: NEW `anvil/lib/parity.py`, NEW `tests/lib/test_parity.py`, MODIFIED `anvil/skills/deck/lib/parity_lint.py` (shrunk to thin re-export), MODIFIED `anvil/skills/memo/lib/parity_lint.py` (shrunk to thin re-export with `lint_source` arg-order wrapper), MODIFIED `tests/skills/deck/test_parity_lint.py` (one docstring test re-pointed at `anvil.lib.parity`), MODIFIED `tests/skills/memo/test_memo_parity_lint.py` (four docstring tests re-pointed at `anvil.lib.parity`), plus this WORK_LOG entry and the [Unreleased] `CHANGELOG.md` entry.
  - **Deferred follow-ons remain queued against `anvil/lib/parity.py`** (they were queued against the skill-local modules and survive the promotion unchanged): semantic-equivalence normalization (`$193K` ↔ `$193,000`), Phase B severity promotion (`warning` → `error`), the standalone `/anvil:parity-check <thread>` command, and centralizing sibling-version discovery inside the wrapper. The Phase B promotion decision is jointly with the deck and memo sides, on the original 2-4-week canary-consumption-signal cadence from PR #205 / PR #215.
  - **Two canary-message variants preserved verbatim**: the deck-side `only_in_memo` diagnostic carries the literal substring `"memo.4 introduced a"`; the memo-side `only_in_deck` diagnostic carries the literal substring `"the symmetric direction"`. Both are grep-pinned by the existing `test_citation_clear_*` tests on each skill side. The shared module dispatches between `_build_message_deck_side` and `_build_message_memo_side` keyed on the `rule` parameter — collapsing into a single unified message function would have broken two grep-pinned regression tests.
  - **`lint_source` argument-order asymmetry preserved.** Deck-side takes `(deck_source, memo_source)`; memo-side takes `(memo_source, deck_source)`. The shared `anvil.lib.parity.lint_source` accepts `(deck_source, memo_source)` positional order with a keyword-only `rule` arg; the memo-side thin re-export wraps it to flip the positional order back. The memo-side symmetry test imports both `lint_source` variants and calls them with opposite arg orders — the wrapper preserves that contract.
  - **`LintResult` carries BOTH `memo_sibling` and `deck_sibling`** after promotion (defaulting to `None`). The deck-side wrapper sets the former; the memo-side wrapper sets the latter; both fields exist on every result so neither skill's `to_summary()` shape breaks. Tests on both sides pin their respective field; the unused field round-trips through `to_summary()` as `null`.

### 2026-06-04

- **Issue #297**: shipped the `anvil:project-migrate` skill — the bridge tool that migrates existing studio projects to the post-#295 / post-#296 canonical model (project root + `BRIEF.md` absorbing all anvil config + `<slug>.md` body filename + `<project>/<slug>/<slug>.<N>/` shape). Closes the third leg of the three-part model lock (#295 + #296 + #297). The skill ships under `anvil/skills/project-migrate/` with the standard SKILL.md + commands/ + lib/ + tests/ scaffolding; rubric.md is intentionally OMITTED per the issue's "migration output is mechanical, Champion may decide rubric-less is fine" carve-out. **Four-module lib decomposition**: `lib/detect.py` (shape detection — recognizes `Shape.PRE_283_CLASSIC` / `Shape.POST_283_ANVIL_JSON` / `Shape.FULLY_MIGRATED` / `Shape.UNKNOWN`; pure read-only walk; vendors the `has_project_brief` predicate to avoid a runtime import dependency on the memo skill), `lib/plan.py` (per-document plan generation — `Plan` / `DocumentPlan` / `ContentRewrite` / `Rename` / `BriefMergeOp` typed; cross-thread reference rewriting via `_find_cross_thread_refs` walks every body for tokens matching old stems and records substitutions against the FINAL target paths so the apply step sequences renames first), `lib/apply.py` (the only mutating module — per-doc snapshot at `<project>/.anvil-migrate-rollback/<slug>/`, atomic per-doc apply with rollback on failure, `git mv` preferred when under git, BRIEF write at the end via temp-file + os.replace), `lib/verify.py` (post-apply round-trip check; granular `VerifyResult` with separate fields for stale `.anvil.json`s, stale skill-fixed bodies, root version dirs, orphaned threads). The `lib/orchestrate.py` ties them into a single `run(project_dir, *, apply, report_only)` entry that the command spec calls. **Three commands**: `/anvil:project-migrate <project-dir>` (dry-run, NO mutations), `/anvil:project-migrate <project-dir> --apply` (execute), `/anvil:project-migrate <project-dir> --report` (markdown report only). **Six test files / 39 tests** under `anvil/skills/project-migrate/tests/`: `test_project_migrate_detect.py` (shape classification across all fixtures), `test_project_migrate_plan.py` (per-shape plan generation including cross-thread ref rewriting + critic-sibling rename), `test_project_migrate_apply.py` (end-to-end pre-#283 → fully-migrated + post-#283 → fully-migrated + bessemer-shaped canary), `test_project_migrate_dry_run.py` (snapshot-and-diff: dry-run never mutates — load-bearing AC), `test_project_migrate_idempotent.py` (re-running --apply on a fully-migrated project is byte-identical zero-diff — the other load-bearing AC), `test_project_migrate_verify.py` (post-apply round-trips through `discover_thread_root` + `load_project_brief` from `anvil/skills/memo/lib/`). **Fixtures** are programmatic builders in `tests/_fixtures.py` rather than baked snapshots: `build_pre_283_classic`, `build_post_283_anvil_json`, `build_fully_migrated`, `build_bessemer_shaped` — each takes a tmp dir and produces a known-shape project tree. The bessemer fixture exercises the canary case (3 memo.N versions with review siblings + an audit sibling + a cross-thread ref "see memo.2 §3" inside memo.3's body). Touched: NEW `anvil/skills/project-migrate/SKILL.md`, NEW `anvil/skills/project-migrate/commands/project-migrate.md`, NEW `anvil/skills/project-migrate/lib/__init__.py` / `detect.py` / `plan.py` / `apply.py` / `verify.py` / `orchestrate.py`, NEW `anvil/skills/project-migrate/tests/__init__.py` / `conftest.py` / `_fixtures.py` / six `test_project_migrate_*.py`, MODIFIED `anvil/skills/README.md` (skill index entry), MODIFIED `CLAUDE.md` (skill count update from 8 to 9), plus this WORK_LOG entry. The install script auto-discovers via `SKILL.md` presence (`find anvil/skills -mindepth 2 -maxdepth 2 -name SKILL.md`) so no `install-anvil.sh` edit is required for the new skill to be installable via `--skills=project-migrate`.
  - **Design trade-off: snapshot rollback boundary is per-document, not per-step.** A failure mid-way through migrating document B rolls back doc B entirely from the per-doc snapshot, then surfaces the error. Doc A is untouched. The alternative (whole-project transaction) would require snapshotting the entire project before any work begins; the per-doc boundary keeps the snapshot small and the rollback fast at the cost of leaving an operator in a "partial migration" state if the apply fails partway through. The per-doc boundary is consistent with the issue's "atomic per doc" acceptance criterion and matches the precedent set by `anvil:memo-migrate` (which is per-thread, not per-portfolio).
  - **Design trade-off: dry-run shape is snapshot-and-diff verified, not reasoned.** The dry-run no-mutation contract is verified by `test_project_migrate_dry_run.py` which computes a SHA-256 hash of every file in the project tree, runs `run(project, apply=False)`, then re-hashes and asserts byte-identity. This is more rigorous than just reading the code and reasoning about which lines write to disk — it catches any future regression where a detector or planner inadvertently calls `.write_text` / `.unlink`. The test covers all four fixture shapes.
  - **Design trade-off: git integration is opt-in via `use_git=True` (the default), not auto-detected per-rename.** The apply module calls `_detect_git_repo(project_dir)` once at the top of `apply_plan` and uses the result for every rename in the plan. The alternative (detecting per rename) would be more robust against partial-repo scenarios (a project root that has `.git/` in only some subtrees) but adds per-rename cost and complicates the unit tests. The single-detect approach matches the precedent set by `memo-migrate`'s `_copy_refs` behavior. Tests use `use_git=False` to exercise the plain-rename path without requiring a real git repo at test time.
  - **Design trade-off: rubric.md OMITTED**. The issue body explicitly carved out: "Skill follows `anvil:<type>` convention; rubric.md optional (migration output is mechanical; Champion may decide rubric-less is fine)." The project-migrate skill produces no scored artifact — its output is a migrated project tree, not a memo / proposal / pub. There is no /40 dimension to score. The SKILL.md frontmatter omits any rubric reference; the install-side skill registration auto-discovers via `SKILL.md` presence and does NOT require a sibling `rubric.md`.
  - **Carve-out from #299 (the `migrate.py` LaTeX bootstrap path)**: documented in the new skill's SKILL.md §"Relationship to `anvil/skills/memo/lib/migrate.py`". `anvil:memo-migrate` continues to write a legacy `.anvil.json` when ingesting a LaTeX memo source — that path produces a thread in the post-#283 with-`.anvil.json` shape. The chosen resolution is **option (b)** from the issue body's carve-out: leave `migrate.py` writing `.anvil.json` but document `project-migrate` as the post-step that consolidates it into the project BRIEF. The composition works without flags or special-casing — `project-migrate` recognizes the post-#283 shape and migrates it the same way it would migrate any other project. A future refactor of `migrate.py` to write BRIEF.md directly is queued but out of scope for #297.
  - **Skill-local first, lib promotion later.** Per CLAUDE.md, every primitive (`detect.py` / `plan.py` / `apply.py` / `verify.py` / `orchestrate.py`) lives under `anvil/skills/project-migrate/lib/`, not under `anvil/lib/`. There is no second consumer for the migration primitives — the skill is one-shot. If a future need surfaces (e.g., per-skill migration tooling that reuses the planner shape), lib promotion to `anvil/lib/migrate/` becomes a follow-on. The detector's `_has_project_brief` predicate is a deliberate **duplication** of the same predicate in `anvil/skills/memo/lib/project_discovery.py` — duplicated so the detector can run in consumer installs without a runtime import dependency on the memo skill, mirroring the precedent set by `refs_pdf.py` (PR #167) and the inlined `PANDOC_REMEDIATION` in `migrate.py`.

### 2026-06-03

- **Issue #232**: fixed the memo weasyprint US-Letter page-size regression by replacing `size: letter;` with the explicit `size: 8.5in 11in;` form in `anvil/lib/memo/styles.css`'s `@page` block. **WeasyPrint 68+ portability gotcha**: the named-keyword `letter` path is silently ignored on at least the macOS Homebrew 68.x build, falling back to A4 (595x842 pts) and violating the README's pinned US-Letter contract. The explicit two-dimension form is portable across WeasyPrint versions and has zero consumer-side cost — `@bottom-center` page-counter and `margin: 0.75in` are preserved unchanged. The `xelatex` fallback path is unaffected (it reads pandoc's `--variable=papersize` chain, not the CSS `@page` rule); CSS is the single source of truth for the weasyprint path. NEW `tests/skills/memo/test_memo_styles_page_size_doc.py` ships a brace-counted `@page`-block reader + two regression assertions: (1) the explicit `8.5in 11in` dim form is present, (2) the bare `size: letter` keyword form is absent — a future revert to the named-keyword shape trips the test at CI time. Distinct test filename per the #58 packaging convention. Touched: MODIFIED `anvil/lib/memo/styles.css`, NEW `tests/skills/memo/test_memo_styles_page_size_doc.py`, plus this WORK_LOG entry. No new Python deps; subprocess-only contract preserved. Companion install-side issues #230 / #231 remain tracked separately.

### 2026-06-02

- **Issue #243 (Phase A)**: shipped the `--plan` / `--apply` two-phase mode on `anvil:memo-revise` — the operator-confirmable change-set preview. The phase split addresses the structural gap surfaced by the studio `raytheon-pitch-strategy` thread on 2026-06-02 (memo.3 → memo.4 → revert): the default-path reviser produced a defensible higher-scoring version, but each addition was defensible only in isolation; the aggregate drifted away from the operator's stated "clean and forceful presentation" intent. A `plan.md` preview between scope choice and edit application lets the operator see per-item summaries (Pericles.2 economics sketch, inline citation pass, Sphere risk #6, "why now" line, §3↔§4 reorder) and decline at line-level before any edit is written. NEW `anvil/skills/memo/templates/plan.md.template` ships the canonical plan-artifact shape (header table + planned-edits table with `ID` / `Source` / `Priority` / `Insertion site` / `Summary` / `Words Δ` / `Dim Δ` columns + aggregate footer with `Target-length flag` ∈ {`within_target`, `exceeds_max`, `under_min`, `no_target`} + optional `Convictions` section). NEW `anvil/skills/memo/tests/test_memo_revise_plan.py` ships 44 doc-coverage + fixture-shape tests across 8 test classes mapping to the seven AC10 cases + a fixture-inventory sanity check + a SKILL.md surface-coverage class. Six NEW fixtures under `anvil/skills/memo/tests/fixtures/memo_revise_plan/` preserve the AC10-named cases as Phase B regression anchors: `clean_plan_apply/` (the baseline `--plan` → `--apply` flow), `stale_verdict_rejected/` (plan-validity case 2 — verdict re-run after plan), `declined_items_decay/` (exercises all three accepted rejection shapes AND preserves the studio canary memo.3 → memo.4 case from the issue body verbatim), `target_length_exceeded/` (footer flag fires on projected overshoot), `polish_plan_compose/` (`--polish --plan` → `--apply` composition with `polish_plan_then_apply` revision_mode), `no_flag_regression/` (intentionally empty — README documents the unchanged-default-path contract). MODIFIED `anvil/skills/memo/commands/memo-revise.md` adds two new flag sections (`--plan` / `--apply`) + a §"Plan-then-apply mode" overview + dispatch steps 0a / 0b at the top of the Procedure block + the plan-sibling `_progress.json` snippet at the bottom + extended the `revision_mode` documentation to cover the two new values (`plan_then_apply` / `polish_plan_then_apply`). MODIFIED `anvil/skills/memo/SKILL.md` adds §"Operator-confirmable change-set preview" sibling to §"Operator-initiated polish passes" + a plan-sibling row in the artifact-contract directory diagram + a state-machine non-gating note + the command-dispatch-table flag update. Touched: NEW `anvil/skills/memo/templates/plan.md.template`, NEW `anvil/skills/memo/tests/test_memo_revise_plan.py`, NEW six fixture directories under `anvil/skills/memo/tests/fixtures/memo_revise_plan/`, MODIFIED `anvil/skills/memo/commands/memo-revise.md`, MODIFIED `anvil/skills/memo/SKILL.md`, MODIFIED `CHANGELOG.md` [Unreleased] entry, plus this WORK_LOG entry.
  - **Phase A scope deliberately constrained.** Reviewer-prose-only — no Python detector module. The CLI flag precedent is `--polish` (issue #201): an operator-side decision shipped as a memo-revise.md spec change with doc-coverage tests, not a Python harness. The plan-parsing logic is small enough to live inline in the command spec; extraction to `anvil/skills/memo/lib/plan.py` is a follow-on once a second consumer adopts the two-phase pattern (or once an automated detector lands for the same regression-test surface the fixtures already preserve). The Phase B trigger is the same as the deck↔memo parity-lint pattern (#215): wait for the second consumer before generalizing.
  - **State-machine non-gating** is load-bearing. A `<thread>.{N+1}.plan/` directory does NOT advance the thread to `REVISED`; the state stays `REVIEWED` until `--apply` writes the matching `<thread>.{N+1}/memo.md`. This preserves the existing immutability contract (a half-built version dir without a `memo.md` is never `REVISED`) and keeps the SKILL.md §"State machine" derivation table byte-identical to pre-this-change. The plan sibling is discoverable by the existing `enumerate_siblings` regex via the `<thread>.{N+1}.plan/` shape (with `_meta.json` declaring `scorecard_kind: "planner"` to disambiguate from `human-verdict` reviewer siblings); aggregation in `anvil/lib/critics.py` skips planner-kind siblings (no per-dimension scores to fold in) so no lib change is required for v0.
  - **Default no-flag path unchanged.** Load-bearing regression contract — every existing consumer (the canary today, the 8 shipped skills' integration tests, the install-script regression tests) MUST NOT break. The two new dispatch steps (0a for `--plan`, 0b for `--apply`) fire FIRST in the Procedure block; absence of both `--plan` and `--apply` falls through to the unchanged 11-step procedure. The 44 tests include a regression class (`TestDefaultNoFlagPathUnchanged`) that pins the step-label invariant (steps 1, 3, 4, 5, 9, 10, 11 verbatim) so a future edit that silently renumbers or deletes a step trips the test.
  - **Composition with `--polish` is bidirectional.** `--polish "<reason>" --plan` writes a polish-pass plan (revision mode field equals `polish` in the plan header; verbatim operator reason recorded as a header subsection); `--apply` reads the plan's revision_mode + operator reason and threads them through to the produced version dir's `metadata.revision_mode = "polish_plan_then_apply"` + `metadata.revise_force_reason = <verbatim>`. The operator does NOT re-pass `--polish` on the `--apply` invocation — the plan IS the audit trail. The `polish_plan_compose/` fixture exercises this composition end-to-end.
  - **`--plan` and `--apply` are mutually exclusive.** Passing both in the same invocation is rejected with a clear error. The two-phase contract is sequential: write the plan, optionally edit it, then apply it. A "fire-and-forget" mode that combines both (write a plan and immediately apply it without operator review) would defeat the purpose of the preview gate.
  - **Three accepted rejection shapes** on `plan.md` (operator edits in place between `--plan` and `--apply`): (1) same-line `<!-- declined: <reason> -->` comment appended to the table row; (2) row deletion (treated as `Resolution: declined — removed from plan` at apply time); (3) `Priority: declined` cell value + `[declined: <reason>]` bracketed addition to the `Summary` cell. Each shape becomes a `Resolution: declined — <reason>` row in `<thread>.{N+1}/changelog.md`. The reason flows verbatim — `--apply` MUST NOT paraphrase or shorten. The shape variety exists because operator editing styles diverge (some operators prefer comments, some prefer cell edits, some prefer row deletion); the parser is small enough to handle all three.
  - **Plan staleness contract (5 cases)**: `--apply` refuses the plan when (1) no matching plan exists, (2) source review `verdict.md` mtime is later than `plan.md`'s (verdict was re-run since plan write), (3) a new critic sibling exists that wasn't recorded in `_progress.json.metadata.critic_siblings_at_plan_time`, (4) `plan.md` mtime is older than `plan_max_age_days` (default 7, configurable via `<thread>/.anvil.json`), or (5) `<thread>.{N+1}/memo.md` already exists. Each rejection points at remediation (typically: re-run `--plan` to refresh). The staleness contract is what makes the two-phase flow safe against the most common drift sources (the operator re-running review or adding a new critic mid-flow).

- **Issue #246 (sub-issue 1 of 4)**: shipped the `gaps.json` schema + `proposal-synthesize` command spec for the `anvil:proposal` synthesis sibling. This is the schema-only foundation for the 3-findings-1-gap problem documented in the canary reproducer (`raytheon-pitch-strategy.1` — three critics all flagged the 12LP+ FinFET mask cost from different angles, the reviser produced a 4-5 line dense footnote addressing each finding separately). The sub-issue decomposition recommended by the curator splits this into four mergeable PRs; this PR is unit 1 (the contract everything else depends on). NEW `anvil/skills/proposal/lib/synthesis_schema.py` ships `GapList` / `Gap` / `ContributingFinding` / `Singleton` pydantic models mirroring `anvil/lib/review_schema.py` shape and discipline (`extra="forbid"`, pinned `schema_version: "1"`, optional fields default to safe empties, `min_length=1` on `contributing_findings`). NEW `anvil/skills/proposal/lib/synthesis_schema.json` is the auto-generated Draft 2020-12 JSON Schema (companion to the pydantic model, generated via `GapList.model_json_schema(...)` per the `anvil/lib/export_schema.py` precedent). NEW `anvil/skills/proposal/commands/proposal-synthesize.md` is the command spec — documents the on-disk shape, resume / crash-recovery contract, clustering procedure (deterministic pre-filter narrows candidates by shared rubric dim / shared section ref / shared evidence span, then LLM does the substantive cluster decision; conservative "leave as singleton when uncertain"), the severity ladder (max-across-contributors; critical-flag → gap `critical`), the aggregator-skip rule (`role: "synthesizer"` in `_meta.json` + no per-dimension scores = the existing aggregator's null-fall-through handles it without any `anvil/lib/critics.py` change), state-machine integration (new `SYNTHESIZED` transient state between `REVIEWED+AUDITED` and `REVISED`), and the reviser-side backward-compatibility fallback (when `gaps.json` is absent, `proposal-revise` reads per-sibling findings directly — preserved as the rollout safety net). NEW `anvil/skills/proposal/tests/test_synthesis_schema.py` ships 23 tests: round-trip on the 12LP+ canary fixture from the issue body, default-fields safety, schema-rejects-invalid coverage (empty contributing-findings; unknown severity; missing required fields; extra fields; `for_version < 1`), full severity vocabulary acceptance, JSON Schema document presence + drift detection between model and on-disk JSON, command frontmatter parse. Distinct test filename per the #58 packaging convention. Touched: NEW `anvil/skills/proposal/lib/__init__.py`, NEW `anvil/skills/proposal/lib/synthesis_schema.py`, NEW `anvil/skills/proposal/lib/synthesis_schema.json`, NEW `anvil/skills/proposal/commands/proposal-synthesize.md`, NEW `anvil/skills/proposal/tests/test_synthesis_schema.py`, plus this WORK_LOG entry and a `CHANGELOG.md [Unreleased]` entry.
  - **Scope deliberately constrained.** Reviser-side consumption of `gaps.json`, orchestrator + state-machine integration in `commands/proposal.md` + `SKILL.md`, and the Studio reproducer integration test are deferred to sub-issues 2 / 3 / 4 of the curator's decomposition. The schema + command spec is the smallest mergeable unit and is the foundation everything else depends on. Sub-issues 2 and 3 can land in parallel once this contract is fixed; sub-issue 4 lands last after the synthesizer behavior is exercised by the orchestrator. Follow-up sub-issues will be filed against `loom:curated` on merge.
  - **Skill-local first, lib promotion later.** Per CLAUDE.md, the schema lives under `anvil/skills/proposal/lib/` not `anvil/lib/`. Other parallel-critic skills (deck, memo, pub, report) have the same N-critics-one-reviser structural shape and could benefit from synthesis, but the lib-promotion trigger is "wait for the second consumer" — defer until a second skill needs it. The schema module docstring explicitly names this discipline. If the studio canary surfaces a 3-findings-1-gap case in memo or deck after sub-issues 2-4 land, that becomes the lib-promotion trigger.
  - **Design notes on the contract.** Three load-bearing decisions worth recording: (1) **`role: synthesizer` is not scored** — the synthesizer is structurally a sibling (for filesystem-discovery uniformity) but its job is to *consolidate* critic output, not to add a new dimension of judgment. The lowest-touch realization is that the synthesizer emits no per-dimension scores; existing aggregation in `anvil/lib/critics.py::aggregate` already drops `None` contributions per-dimension, so no lib change is required for v0. Explicit-skip wiring is optional and may be added when a second skill adopts the role. (2) **Severity is `max` across contributors, not mean.** A `nit` from review + a `blocker` from audit → gap `blocker`. Averaging severity has no meaningful midpoint and would dilute the load-bearing signal of a critical-flag contributor. (3) **Backward-compatibility fallback is essential.** When `gaps.json` is absent at revision time, `proposal-revise` MUST fall back to reading raw critic siblings directly. This is the rollout safety net so existing in-flight threads continue to work and so per-thread opt-out via `<thread>/.anvil.json` is structurally possible if a consumer chooses to defer adoption. The reviser-side wiring sub-issue (#2) is what makes this concrete; this PR only pins the contract.
  - **Companion to issues #241 and #244.** Synthesis directly addresses one of #241's three downstream consequences ("synthesis-shaped findings hit additively"). The other two — every-nice-to-have-lands, no-scope-control — remain orthogonal concerns. Synthesis severity tags and the proposed dim-9 rhetorical-economy rubric pressure (#244) are complementary: synthesis reduces the *volume* the reviser sees, the rhetorical-economy dim adds *cost* for layered language. Both are documented in the command spec's "Companions and related work" section.

- **Issue #227 / kill switch on #147**: reverted the `_convictions.md` advisory primitive from `anvil:memo` per the Phase B kill-switch contract documented in PR #155's WORK_LOG entry. The architect's two-week canary observation window (Epic #142, shipped via #147 on 2026-05-31) surfaced two structural defects, both load-bearing: **#225** — the named consumer (next reviser at v{N+1}) only runs when `verdict.advance == false`, so an `advance:true` memo never produces a reader of its own ledger file (Citation Clear memo.4 wrote one; memo.5 will never exist; the file is dead-letter by lifecycle design); and **#226** — the strict `Resolution: declined` trigger catches only stylistic nits, missing the load-bearing addressed-with-judgment-held positions that are the actual carry-forward shape. The canary evidence across the studio corpus was 1 written ledger (Citation Clear memo.4), 7+ revisers explicitly declining to write one, and 0 read-back signals. Reverted the four files touched by #147 (`anvil/skills/memo/SKILL.md` §Convictions ledger subsection + version-dir diagram entry + stale polish-pass cross-reference, `anvil/skills/memo/commands/memo-revise.md` steps 7.5 + 9.5 + stale audit-trail cross-reference + struck the inline mention in step 9.7's file enumeration, `anvil/skills/memo/templates/BRIEF.migration.md.example` §Convictions section, deleted `tests/skills/memo/test_memo_convictions_ledger_doc.py`) and reworded four cross-references in downstream PRs (`anvil/skills/deck/lib/parity_lint.py`, `anvil/skills/deck/commands/deck-review.md`, `anvil/skills/memo/lib/parity_lint.py`, `anvil/skills/memo/commands/memo-review.md`, and the analogy comments in `tests/skills/memo/test_memo_revise_polish_flag.py`) that had taken a hard cite-dependency on the §"Convictions ledger" doc anchor. Epic #158 / Phase 3's `memo-render` step 9.7 in memo-revise.md is preserved unchanged; only the inline ledger-file mention in its written-files enumeration was struck. Also gave the `.latest` convenience-symlinks block in SKILL.md its own `### Optional .latest convenience symlinks` heading (it had been visually nested inside §"Convictions ledger" without its own header; the deletion would have left it orphaned).
  - **Pattern recorded**: this is the third negative-result revert in anvil (precedent: PR #40 inline-mermaid claim corrected by PR #72; #21 / #100 umbrella decompositions). The pattern is: ship a load-bearing claim or primitive with an explicit falsifiability contract (here: single named consumer + bounded observation window + named kill-switch criterion), observe canary signal, retract cleanly when the signal contradicts the contract. The architect proposal #142 worked — correctly identified a problem, designed a falsifiable v0, built in the kill switch. The primitive did not survive observation. The underlying cross-version conviction carry-forward problem is real and will be re-architected in a separate `loom:architect` issue filed alongside #227.
  - **Cross-skill citation hygiene**: the `_convictions.md` Phase A/B kill-switch precedent had been cited as a pattern-precedent by downstream work (deck parity-lint in PR #205's Phase A/B framing; the memo-side parity mirror in PR #215; the polish-pass advisory-only framing in #201). Those downstream cites are reworded to point at the kill-switch *outcome* (this PR + this entry) rather than the deleted SKILL.md §Convictions ledger anchor. The pattern itself — ship-with-falsifiability, observe, retract-cleanly — is now a meta-precedent worth citing on its own.

### 2026-06-01

- **Issue #215** (Phase A, queued follow-on #1 of 4 from PR #205): shipped the **memo-side mirror** of the deck↔memo parity pre-flight lint as a `memo-review` sub-step. New skill-local module `anvil/skills/memo/lib/parity_lint.py` is a **near-byte-identical mirror** of `anvil/skills/deck/lib/parity_lint.py` (PR #205) with the "primary artifact" framing flipped — `lint_source(memo_source, deck_source)` takes memo first, the rule label is `memo_deck_parity` (escape: `<!-- anvil-lint-disable: memo_deck_parity -->`), and `LintResult.deck_sibling` mirrors the deck-side's `memo_sibling`. The `Finding.side` values (`"only_in_memo"` / `"only_in_deck"`) are preserved verbatim — they describe *which body the token came from*, independent of which side is "primary". Extractors, normalization, lint-disable parsing, and result shape are byte-identical to deck-side. Touched: NEW `anvil/skills/memo/lib/parity_lint.py`, NEW `tests/skills/memo/test_memo_parity_lint.py` (34 unit + symmetry + doc-coverage tests including the load-bearing AC12 symmetry test that validates the memo-side and deck-side `lint_source` calls produce equivalent finding sets on the same body pair), `anvil/skills/memo/commands/memo-review.md` step 4d (between current 4c render-gate read and step 5 dimension scoring) + step 9 `_summary.md.lint.memo_deck_parity` block + step 9 `findings.md` `## Parity-lint findings (memo↔deck, optional)` subsection (always emitted, three shapes: clean / with findings / skipped) + explicit non-participation in `critical_flag` note, plus this WORK_LOG entry.
  - **Second-consumer trigger fires with this PR's merge** for promotion to `anvil/lib/parity.py`. Per CLAUDE.md §"Skill-local first, lib promotion later" — "wait for the second consumer before generalizing." With both `anvil/skills/deck/lib/parity_lint.py` (PR #205) and `anvil/skills/memo/lib/parity_lint.py` (this PR) now on disk, the trigger condition is satisfied for the first time. The follow-up promotion is queued as PR #205 follow-on #2 of 4 — to be filed immediately on merge. The canonical promotion is the one-line import-path swap the module docstrings already name: both `parity_lint.py` modules become thin re-exports of the shared `anvil.lib.parity` module, with a small `lint_parity(primary_path, sibling_path, primary_kind: Literal["deck","memo"], sibling_kind: Literal["deck","memo"])` wrapper to centralize the sibling-discovery convention.
  - **Phase A warning-only status carried verbatim from deck-side**: v0 ships at `warning` severity. Parity findings do NOT contribute to `lint_critical_flag`, do NOT touch the verdict aggregation in step 7, and do NOT force `advance: false`. The `errors` list on the result is always empty in v0; the field is preserved for shape-parity with `marp_lint.LintResult` and the deck-side `parity_lint.LintResult` so a future Phase B promotion to `error` severity does not require a schema change. The memo-review verdict aggregation is byte-identical to a thread with the parity lint disabled — AC10.
  - **Phase B promotion decision**: deferred 2–4 weeks after this PR's merge, jointly with the deck-side. Same decision criteria as deck-side (PR #205): (1) does the canary surface reliably-actionable warnings on revise passes? (2) is the false-positive volume manageable, or does it need the deferred semantic-equivalence normalization layer first? PR #205 follow-on #3 of 4 (Phase B severity promotion) is the canonical next step.
  - **Symmetry contract** (AC12, load-bearing): the memo-side and deck-side `lint_source` calls produce equivalent finding sets on the same body pair (modulo the rule names `memo_deck_parity` vs `deck_memo_parity`). Running deck-review and memo-review on the same `<thread>.{N}` produces the same warning set with the same tokens, just under different rule names — the contract that justifies the lib-promotion follow-up. Test `test_symmetry_with_deck_side_lint_source` pins this contract on the Citation Clear canary body pair; `test_symmetry_with_synthetic_body_pair` tightens it on a synthetic case where both `only_in_memo` and `only_in_deck` directions fire.

- **Issue #200** (Phase A): shipped the deck↔memo parity pre-flight lint as a `deck-review` sub-step. New skill-local module `anvil/skills/deck/lib/parity_lint.py` exposes `Finding` / `LintResult` / `_LINT_DISABLE_RE` / `lint_source(deck_source, memo_source)` / `lint_deck_memo_parity(deck_version_dir, memo_version_dir)`, mirroring `anvil/skills/deck/lib/marp_lint.py` and `anvil/skills/memo/lib/memo_image_refs.py` exactly so promotion to `anvil/lib/parity.py` (once the memo-side mirror lands as the second consumer) is a one-line import-path swap. Extractors: money (`$XXK/M/B`, decimal prices), percentages (en-dash and ASCII ranges), quarters/FY tags (`Q1 FY24`, `FY2024`), named months + year (`Jan 2025`, `April 2024`), ALL-CAPS acronyms (length 2-6 to reject prose shouting), unit-bearing integers (`8 pilots`, `50 LOIs`). Token normalization is deliberately minimal (en-dash → hyphen, whitespace collapse, money magnitude uppercase, trailing period strip); semantic-equivalence matching (`$193K` ↔ `$193,000`) is explicitly deferred to a follow-on. Canary anchor: Citation Clear memo.4 ↔ deck.3, where the reviser introduced a load-bearing insurer benchmark "~50–60% completion" into memo.4 that deck.3 lacked and no anvil primitive detected the drift — the regression test `test_citation_clear_canary_50_60_percent_completion_flagged` pins exactly this shape. Wired into `anvil/skills/deck/commands/deck-review.md` as new step 5d with the graceful-skip-on-no-memo-sibling contract (records skip in `_summary.md.lint.deck_memo_parity` with `ran: false` and an explicit reason so the operator sees WHY the check did not fire — same convention as `auto_shrink` skip-reason), `_summary.md` step 9 carries a structured `lint.deck_memo_parity` block, and `findings.md` step 11 carries a `## Parity-lint findings (deck↔memo, optional)` subsection that is **always present** (clean / skipped / divergence). Escape hatch `<!-- anvil-lint-disable: deck_memo_parity -->` (same-line or line-above) downgrades findings from `warning` to `info`. Touched: NEW `anvil/skills/deck/lib/parity_lint.py`, NEW `tests/skills/deck/test_parity_lint.py` (24 unit + doc-coverage tests covering the canary regression, graceful-skip, escape-hatch, every extractor, and the deck-review.md wiring), `anvil/skills/deck/commands/deck-review.md` step 5d + step 9 (`_summary.md` JSON shape) + step 11 (`findings.md` subsection) + step 12 (explicit "parity findings do NOT participate in critical-flag aggregation in v0" note), plus this WORK_LOG entry.
  - **Phase A warning-only status**: v0 ships at `warning` severity. Parity findings do NOT contribute to `lint_critical_flag`, do NOT touch the verdict aggregation in step 12, and do NOT force `advance: false`. The `errors` list on the result is always empty in v0; the field is preserved for shape-parity with `marp_lint.LintResult` so a future Phase B promotion to `error` severity does not require a schema change. The deck-review verdict aggregation is byte-identical to a thread with the parity lint disabled.
  - **Phase B promotion decision**: deferred 2–4 weeks after this PR's merge, based on canary consumption signal — does the warning fire reliably on actionable drift, or is the false-positive volume too high? Mirrors the `_convictions.md` Phase A/B kill-switch precedent from issue #147 / `anvil/skills/memo/SKILL.md` §"Convictions ledger". The Phase B decision criteria are: (1) does the canary surface a reliably-actionable warning on revise passes? (2) is the false-positive volume from same-value-different-token pairs (`$193K` ↔ `$193,000`) manageable, or does it need the deferred semantic-equivalence normalization layer first? Decision recorded here when made.
  - **Lib promotion deferred** per the CLAUDE.md "skill-local first, lib promotion later" pattern. The module docstring explicitly names `anvil/lib/parity.py` as the promotion target and notes the one-line import-path swap; the memo-side mirror (`anvil/skills/memo/lib/parity_lint.py`) is the named second-consumer trigger.

### 2026-05-31

- **Issue #167**: added the optional `pdftotext` subprocess PDF refs back-check path to `anvil:memo`, closing the v0 gap from PR #162 / issue #144 where PDF source-of-truth refs were presence-only because anvil shipped no PDF text extraction. New skill-local helper `anvil/skills/memo/lib/refs_pdf.py` exports `check_pdftotext_available()`, `extract_pdf_text(pdf_path)`, and `PDFTOTEXT_REMEDIATION` — mirrors the `check_mmdc_available` (#65) / `check_pdfjam_available` (#85) / `check_auto_shrink_deps_available` (#102) preflight family in `anvil/lib/render.py`, reuses `RenderError` from the same module for cross-framework exception consistency. **No `pyproject.toml` change** — `pdftotext` is a subprocess CLI binary (poppler-utils), not a Python dep; Path B (pypdf / pdfplumber Python dep) is explicitly deferred per the issue body. Graceful-degrade contract is load-bearing: when `pdftotext` is absent, both `memo-draft` (drafter) and `memo-review` (reviewer) fall back to the **exact** v0 presence-only behavior shipped in PR #162; the reviewer additionally records an info-level `_summary.md.lint.refs_pdf_extraction` block carrying the install story so the consumer sees how to enable the back-check on the next run. Empty-extraction (image-based / scanned PDFs) is also handled gracefully — `pdftotext` returns an empty string, the helper does not treat it as an error, and the reviewer logs an info-level note + falls back to presence-only handling for that specific file with no rubric deduction. Touched: NEW `anvil/skills/memo/lib/refs_pdf.py`, NEW `tests/skills/memo/test_memo_refs_pdf_extraction.py` (24 unit + doc-coverage tests, monkeypatched `shutil.which` / `subprocess.run` so no real poppler at test time), `anvil/skills/memo/SKILL.md` §"Source-of-truth materials", `anvil/skills/memo/commands/memo-draft.md` step 3, `anvil/skills/memo/commands/memo-review.md` step 5 + step 9 (the `_summary.md.lint.refs_pdf_extraction` block shape), plus this WORK_LOG entry. Lib promotion to `anvil/lib/memo/` deferred until `anvil:report` / `anvil:pub` / `anvil:proposal` grow analogous `refs/` source-of-truth contracts (skill-local first per CLAUDE.md).
- **Issue #147** (Epic #142 / Phase A): added the narrow `_convictions.md` advisory contract to `anvil:memo`. The reviser writes settled convictions after the changelog step; the next reviser reads them before re-litigating positions that have already survived an explicit critic challenge or an explicit reviser decision. Each conviction MUST name a body anchor (section heading or paragraph) in the current `memo.md` — a conviction whose anchor no longer exists is automatically stale. Advisory only: not scored, not gating, no state-machine impact; the reviewer does not read it. Touched four files: `anvil/skills/memo/SKILL.md` (version-dir diagram + new §Convictions ledger subsection), `anvil/skills/memo/commands/memo-revise.md` (steps 7.5 read + 9.5 write), `anvil/skills/memo/templates/BRIEF.migration.md.example` (shape demonstration), plus this WORK_LOG entry. No `anvil/lib/` extraction (Phase D), no other skills modified (Phase C), no rubric changes (separate concern per architect proposal #142).
  - **Phase B kill switch**: if the canary does not consume `_convictions.md` within 2–4 weeks of this PR's merge, close this work entirely — delete the addition from all four touched files and document the negative result here per the PR #40 / PR #72 precedent. The single named consumer is the *next reviser at the next version*; if that consumer never reads the file, the contract has no audience. The architect's risk mitigation is exactly this falsifiability (single named consumer + bounded observation window), and the body-anchor requirement is the safeguard that keeps the ledger tied to the current artifact rather than drifting into general project memory.

### 2026-05-30

- **PR #105**: feat(deck): silent Marp auto-shrink detector + Anvil's first `pyproject.toml` (closes #102)
- **PR #104**: feat(deck): `figure-italic-supporting-line-too-long` lint + word budget (closes #101)
- **PR #103**: docs(readme): update to reflect installable v0.0.1 with 8 shipped skills
- **PR #99**: feat(figures): per-glyph Unicode fallback + 4 semantic mermaid classDefs (closes #92)
- **PR #98**: docs(ip-uspto): normalize critic phase names to use critic tag (closes #88)
- **PR #97**: fix(install): error on bare `--skills=` (closes #82)
- **PR #96**: docs(ip-uspto): clarify Check 7 is string-presence, cross-ref Check 9 (closes #87)
- **PR #95**: fix(slides): pdfjam OPTIONAL + `.0.outline` orchestrator exemption (closes #85)
- **PR #94**: fix(report): reviewer enforces PDF existence + freshness for Dim 7 (closes #84)
- **PR #93**: fix(install): make `--dry-run` honest about what it did not do (closes #81)
- **PR #91**: fix(report): strengthen ack-file matching + flag unreachable citations as critical (closes #83)
- **PR #90**: fix(deck): reconcile slide-order across drafter, critics, archetypes (closes #86)
- **PR #89**: fix(install): remove `sh -c` indirection so quoted paths survive (closes #80)
- **Issue #100** (umbrella, closed): decomposed into #101 + #102 — two residual overflow modes in the post-#68 figure idiom surfaced by 13-deck canary re-render wave
- **Issue #21** (umbrella, closed): decomposed into 9 focused issues (#80–#88) — PR-wave follow-ups from #15–#20
- **Issue #106** (opened, follow-up to #105): declare `pydantic` in `pyproject.toml`; `anvil/lib/__init__.py` eagerly imports `cite` which requires it

### 2026-05-29

- **PR #79**: feat(lib): render-gate primitive + wire into 5 paginated skills (closes #64) — `anvil/lib/render_gate.py` (page-fit, overfull, compile, placeholder); wired into pub/report/installation/proposal/ip-uspto; memo OUT of scope per maintainer (markdown-first)
- **PR #78**: fix(slides): correct mermaid-PDF claim in `slides-revise.md` (closes #77)
- **PR #76**: fix(slides): wire `mmdc` preflight + strengthen smoke test (closes #70) — slides-side of the #32/PR #40 inline-mermaid regression
- **PR #75**: test(deck): add #50 white-on-white ask-table regression armor (closes #57)
- **PR #74**: feat(figures): ship `anvil/lib/figures/` shared brand-palette primitive (closes #69) — `palette.py` + `anvil.mplstyle` + `mermaid-theme.json` + CSS-drift guard test
- **PR #72**: fix(deck): make mmdc-rendered PNG the working diagram path; preflight + dep check (closes #65) — corrected #32's false inline-mermaid claim across deck + slides docs; added `mmdc` preflight; added `install-anvil.sh --check-deps`
- **PR #68**: fix(deck): replace overflow-prone figure+bullets and ask H1+H2 idioms in template (closes #24, #25)
- **PR #67**: docs(deck): add matplotlib `figure-conventions.md` and wire cross-references (closes #23)
- **PR #66**: docs(snippets,memo): fix phantom phase state and tighten `_meta.json` contract docs (closes #36) — contract bug in `progress.md` snippet
- **PR #63**: test: complete `__init__.py` package chain for deck/slides/proposal test dirs (closes #58)
- **PR #62**: feat(proposal): implement `anvil:proposal` skill (buildable-system proposals) (closes #60)
- **PR #61**: feat(installation): implement `anvil:installation` skill (experiential artwork) (closes #59)
- **Issue #51** (umbrella, closed): decomposed into #59 + #60 — new artifact classes for non-investment studio works
- **PR #56**: feat(ip-uspto): add `ip-uspto-vision` VLM critic for patent drawings (closes #48)
- **PR #55**: fix(deck): make ask-slide tables readable by defeating Marp default cell bg (closes #50)
- **PR #54**: feat(report): add `report-vision` VLM critic for rendered PDFs (closes #47)
- **PR #53**: feat(pub): add `pub-vision` VLM critic for rendered research papers (closes #46)
- **PR #52**: feat(slides): add `slides-vision` VLM critic command + tests (closes #45)
- **PR #49**: feat(lib): VLM critic primitive + `deck-vision` sibling (closes #30) — `anvil/lib/render.py` + `anvil/lib/vision.py` + first consumer
- **PR #44**: feat(pub): venue-pinned advisory rubric overlays alongside generic /40 (closes #33) — `anvil/lib/rubric.py` + neurips/nature/arxiv YAMLs
- **PR #43**: docs(lib): codify `.review/` vs `.audit/` as CRITIC judgment-vs-tool-evidence split (closes #29) — new `anvil/lib/snippets/audit.md`
- **PR #42**: feat(lib): add stable-score termination as secondary stop condition (closes #27) — `anvil/lib/convergence.py` with `STALLED` verdict
- **PR #41**: feat(lib): shared citation primitive `anvil/lib/cite.py` (closes #28) — DOI + arXiv resolvers, stdlib-only, idempotent `refs.bib`
- **PR #40**: feat(lib): document MathJax + `--html`/mermaid figure pipeline as canonical Marp config (closes #32) — *NOTE: this PR shipped the inline-mermaid-default design that turned out to be wrong; corrected by PR #72 a day later*
- **PR #39**: feat(lib): canonical `_review.json` schema, discovery + aggregation (closes #26) — `review_schema.py` + `critics.py` with legacy adapters
- **PR #38**: feat(deck,slides): wire `slide-content-overflow` lint into review phase (closes #31)
- **PR #37**: feat(ip-uspto): add `_outline.json` control surface for chunked drafting (closes #34)
- **PR #35**: feat(lib): extract `anvil/lib/snippets/` and migrate 6 skills (closes #10) — markdown-snippet convention layer

### Earlier (skills v0 implementation phase)

- **PR #20** → closed #4: implement `anvil:ip-uspto` skill
- **PR #19** → closed #6: implement `anvil:deck` skill (Marp)
- **PR #18** → closed #7: implement `anvil:slides` skill (Marp)
- **PR #17** → closed #8: implement `anvil:report` skill
- **PR #16** → closed #5: implement `anvil:pub` skill
- **PR #15** → closed #11: implement `scripts/install-anvil.sh`
- **PR #13** → closed #3: implement `anvil:memo` skill
- Initial commit + scaffolding

---

## Maintenance notes

- **PR #40 (canary-introduced regression)**: this PR shipped #32's inline-mermaid-as-default design. The studio canary's 12-deck render wave (resolved #65/#72) proved the claim false — inline mermaid leaks as raw code in the rendered PDF; `mmdc → PNG` is the only working diagram path. Both smoke tests at the time only checked "non-empty PDF + exit 0", which is why it slipped through review. Lesson recorded in #70: *"renders to a non-empty PDF" ≠ "renders correctly"*.
- **Decomposition pattern**: Three umbrellas were decomposed during this period (#21 → 9 issues; #51 → 2; #100 → 2). Decomposition is preferred over bundled umbrellas when items have different risk profiles or touch independent files.
- **Curator-skip pattern**: For one-line / mechanically-checkable fixes, we skipped the curator phase and built directly from the issue body (#77, #82, #87, #88). Same for direct-verify in lieu of a judge agent (#77, #87, #97, #98, #103). Reserve agent dispatch for substantive code review.
- **#105 introduced Anvil's first `pyproject.toml`**. The deps philosophy is documented in its top comment: subprocess-only by default; Python deps are optional extras with preflight + remediation.
