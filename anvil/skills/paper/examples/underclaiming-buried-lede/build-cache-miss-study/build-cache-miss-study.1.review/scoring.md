# Scoring — build-cache-miss-study.1

Rubric: `anvil-pub-v2` (/44). Advance threshold: ≥35.

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | Attribution is defined precisely enough to reproduce and its one modelling choice is named rather than hidden: `("A miss is attributed to a source file when that file's content hash appears in the action's resolved input set and differs from the hash present at the last observed hit for the same action identity" — §3.1)`. Temporal leakage is handled explicitly: `("five-fold cross-validation grouped by week to avoid temporal leakage" — §3.2)`. Nothing in the method overreaches what the trace can support. |
| 2 | Evidence sufficiency | 6 | 6 | The ablation isolates each signal's contribution and the interpretation is supported rather than asserted: `("The two signals select different files. Churn ranks recently edited leaves highly; centrality ranks stable files whose edits propagate widely." — §4.3)`. A randomized deployment, a baseline from prior work, a four-row ablation, and a sensitivity re-run for the attribution choice are all present. |
| 3 | Clarity of contribution | 5 | 1 | **Underclaiming / buried-lede (cold-reader check).** The abstract and introduction yield only the measurement and the prototype; the organizing idea the brief names as the strongest honest statement (history as a build-scheduling signal) surfaces once, in the Discussion, disowned in the same sentence: `("We do not develop that framing here and leave it to future work." — §5)`. The abstract's closing move demotes both remaining items to provisional status: `("The characterization is descriptive, the prototype is preliminary, and we do not claim that either result generalizes beyond the environment measured here" — abstract)`. A cold reader can state what the authors measured but not what they think is true. Scored at 1, not 0: the contribution is present in the paper, merely demoted. |
| 4 | Related-work positioning | 5 | 3 | Positioning is honest but collapses the paper's synthesis claim to its least-novel ingredient: `("Our ranking function is a straightforward re-application of these measures to a new input, and we claim no novelty in the predictor itself." — §2)`. The predictor is indeed inherited; the composition of history mining with online scheduling is not, and the section never positions at that scope. Component-novelty calibration per `commands/paper-litsearch.md`. Each cluster is otherwise engaged on its merits with no close prior work ignored. |
| 5 | Reproducibility | 5 | 5 | Seeds, folds, regularization, feature windows, the CPU budget, and the released artifact are all specified: `("a fixed seed (20260214) recorded in the released artifact" — §3.2)`. An independent group with the released trace could re-fit the predictor and re-derive the tables. |
| 6 | Figure & table quality | 4 | 3 | Both tables are correctly aligned with meaningful headers, and the concentration caption defines its own key term standalone. Deducted 1: the ablation caption does not tell its story without the body — `("Latency reductions are medians over the deployment arm described in Section~\ref{sec:experiments}." — Table 2 caption)` names where the numbers come from but not what the reader should conclude from the two-row-versus-combined comparison. No rendered figures exist at this version. |
| 7 | Prose & structural quality | 4 | 3 | Tense and voice are consistent and the LaTeX is clean (no broken cross-references, no overfull boxes reported). Deducted 1: the introduction spends two full paragraphs on defensive apparatus before any claim — `("Measurement of production build clusters is difficult for reasons that constrain what can responsibly be claimed from it." — §1)` opens the second paragraph, and the third continues it, so the section's flow reaches its own contributions only after the reader has been told twice not to expect much. No self-flattering virtue-signalling adjective and no structural-importance announcement were found in the body. |
| 8 | Citation hygiene | 5 | 5 | Every non-trivial claim carries a citation and every `\cite{}` resolves to a complete `refs.bib` entry: `("standard at organizations above a few hundred engineers~\cite{okonkwo2019remote}" — §1)` is representative. Author, title, venue, year, pages, and DOI are present on all entries; claim-support verification is `paper-audit`'s half. |
| 9 | Rhetorical economy | 4 | 1 | **Burying the lede is a rhetorical-economy failure** per `rubric.md` §"Dim 3 / Dim 9". The reader pays for two paragraphs of scope-limiting before reaching a claim: `("We are likewise constrained. Our three repositories belong to a single organization, share a single CI configuration lineage, and use one build tool." — §1)`. Hedging exceeds the paper's actual uncertainty — the deployment is randomized and the ablation is clean, yet the abstract still withholds. A busy reviewer cannot extract the contribution in 90 seconds because the contribution the authors care about is in the fifth section. Scored at 1: the prose itself is economical sentence by sentence, so this is a placement failure rather than padding. |
| | **Total** | **44** | **33** | Below the ≥35 advance threshold (33/44). No critical flags. The named underclaiming / buried-lede finding is recorded in `comments.md` at `blocker` severity. |
