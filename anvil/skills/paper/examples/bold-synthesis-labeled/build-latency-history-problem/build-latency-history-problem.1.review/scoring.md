# Scoring — build-latency-history-problem.1

Rubric: `anvil-pub-v2` (/44). Advance threshold: ≥35.

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Rigor of method / argument | 6 | 6 | Attribution is defined precisely enough to reproduce and its one modelling choice is named rather than hidden: `("A miss is attributed to a source file when that file's content hash appears in the action's resolved input set and differs from the hash present at the last observed hit for the same action identity" — §3.1)`. Temporal leakage is handled explicitly: `("five-fold cross-validation grouped by week to avoid temporal leakage" — §3.2)`. The method section is byte-identical to the sibling fixture's, and scores identically. |
| 2 | Evidence sufficiency | 6 | 6 | The ablation isolates each signal's contribution and the interpretation is supported rather than asserted: `("The two signals select different files. Churn ranks recently edited leaves highly; centrality ranks stable files whose edits propagate widely." — §4.3)`. A randomized deployment, a baseline from prior work, a four-row ablation, and a sensitivity re-run for the attribution choice are all present. Identical evidence to the sibling fixture. |
| 3 | Clarity of contribution | 5 | 5 | The contribution is extractable in one sentence from the abstract's first line: `("We argue that a distributed build system should treat version-control history as a scheduling input, not merely as metadata for code analysis." — abstract)`, and the contribution list gives one sentence per item under an explicit evidentiary label. **No overclaiming deduction taken**: per `rubric.md` §"Ambition is not novelty inflation", staking a synthesis claim while labelling which ingredients are inherited is the paper doing its job, and the conjecture is marked as one — `("We expect the same history signal to govern regression-test selection and CI-runner placement" — §1)` is stated with a falsifiable prediction rather than as a result. |
| 4 | Related-work positioning | 5 | 5 | Each cluster is engaged on its merits, inherited ingredients are named as inherited — `("We inherit the warming mechanism from the first and the budget-controller shape from the second." — §2)` — and the section still positions the paper at the scope of its actual claim rather than collapsing to the least-novel component. The closest prior instance of history driving an online decision is engaged rather than dismissed on scope. |
| 5 | Reproducibility | 5 | 5 | Seeds, folds, regularization, feature windows, the CPU budget, and the released artifact are all specified: `("a fixed seed (20260214) recorded in the released artifact" — §3.2)`. An independent group with the released trace could re-fit the predictor and re-derive the tables. |
| 6 | Figure & table quality | 4 | 3 | Both tables are correctly aligned with meaningful headers, and the concentration caption defines its own key term standalone. Deducted 1: the ablation caption does not tell its story without the body — `("Latency reductions are medians over the deployment arm described in Section~\ref{sec:experiments}." — Table 2 caption)` names where the numbers come from but not what the reader should conclude. This is the pair's one shared deduction; both fixtures carry the same two tables and no rendered figures. |
| 7 | Prose & structural quality | 4 | 4 | Flow is intact and the contrast that carries the argument is stated once, in ordinary language, before any formalism: `("the dependency graph says what \emph{could} change, while version-control history says what \emph{will}" — §1)`. Tense and voice are consistent, cross-references resolve, and no hand-waving stands in for an argument. No self-flattering virtue-signalling adjective and no structural-importance announcement were found in the body. |
| 8 | Citation hygiene | 5 | 5 | Every non-trivial claim carries a citation and every `\cite{}` resolves to a complete `refs.bib` entry: `("Content-addressed caching for build actions is mature~\cite{okonkwo2019remote}" — §2)` is representative. The bibliography is byte-identical to the sibling fixture's; claim-support verification is `paper-audit`'s half. |
| 9 | Rhetorical economy | 4 | 4 | The most important claim is surfaced first, in the title and in the abstract's opening sentence, and the abstract states what the evidence buys in the same breath as what it does not: `("the claim is that their composition makes history a scheduling signal, and the ablation supports it" — abstract)`. Hedging is proportional — the conjecture is hedged, the deployment result is not — and a busy reviewer extracts the contribution well inside 90 seconds. No paragraph restates another. |
| | **Total** | **44** | **43** | Above the ≥35 advance threshold (43/44). No critical flags. No underclaiming finding and no overclaiming deduction. |
